# Development log - 2026-07-22

## 03:37 CDT - Lane Graph V2 complete

- Replaced uniform lane generation with shared mitered offset geometry, measured road clearance,
  lane fan-in/fan-out, stop-line setbacks, and legal junction movement generation.
- Added five road classes (`arterial`, `boulevard`, `street`, `service`, and `alley`) with authored
  speed, lane layout, route-priority, and traffic-density metadata exposed in the level editor.
- Added congestion-aware A* routing using per-tick edge occupancy while retaining closures as hard
  constraints and keeping the authoritative server in control of traffic decisions.
- Regenerated BIL as 372 corridors, 213 junctions, 3,084 directed lane nodes, and 4,015 edges. The
  graph covers 15,569 of 15,793 road cells and includes 179 multi-lane corridors.
- Added exact compiled-lane, direction, clearance, stop-line, and movement overlays so authors can
  inspect the network the runtime actually consumes.
- Fixed traffic recovery so a stopped oncoming car cannot be ignored by generic pass/unstick logic,
  which previously allowed persistent vehicle overlap at opposed approaches.
- QA: TypeScript, production build, focused traffic-controller tests, and the traffic soak pass.
  The final full-suite run passed 606 of 607 tests; its timing-sensitive multiplayer integration
  scenario timed out under full-suite contention and then passed in isolation in 23.2 seconds.

## 04:26 CDT - Generated traffic is explicitly one-way

- Replaced every generated legacy `both` corridor with a paired `forward` and `reverse` one-way
  carriageway. The runtime still compiles 3,084 lane nodes, so this changes authoring ownership rather
  than doubling simulated traffic capacity.
- Added explicit terminal transfer junctions and U-turn movements for all 41 dead ends, preserving
  strong connectivity without making terminal roads bidirectional.
- Rebound roadblocks against the nearest carriageway supporting each blocked direction.
- Regenerated BIL as 744 one-way carriageways, 254 junctions, and 4,019 directed edges. The generated
  asset contains 372 forward, 372 reverse, and zero bidirectional corridors.
- Fixed explicit terminal U-turn connectors to retain their `turnaround` edge kind and exposed U-turn
  counts in the junction inspector.
- Updated the occupied-junction traffic fixture to select a normal crossing movement instead of the
  first movement in the graph, which is now correctly a terminal U-turn. This keeps crossing-zone
  contention and terminal-transfer behavior covered by separate tests.
- QA: map validation, all 27 focused generator/compiler/graph/traffic-controller tests, TypeScript,
  the production Next.js build, and the complete 608-test suite pass. The full suite completed in
  26.6 seconds.
- Browser QA confirmed that previously saved editor drafts remain intentionally unchanged and can
  still show legacy `BOTH` records. Running **Generate full road network** is the explicit migration
  path; repository data itself contains zero bidirectional corridors.
