# 2026-08-21

## 03:38 AST - Combat response and vehicle history parity

- Traced delayed shooting feedback to a fully server-gated presentation path: the client
  sent fire intent but rendered no projectile or recoil until the authoritative snapshot.
- Added correlated provisional projectile IDs to combat protocol v9. The local client now
  renders eligible bullet shots immediately and hands each visual off to, corrects it from,
  or removes it based on the authoritative fire receipt and replicated bullet state.
- Kept ammo consumption, fire-rate validation, lag compensation, collision, hits, damage,
  death, and projectile persistence exclusively server-authoritative. Malformed, duplicated,
  rejected, resolved, and timed-out provisional IDs fail closed and are removed.
- Restored the driven-vehicle saved-input history from 24 ticks (about 400 ms at 60 Hz) to
  a time-based 3.2-second window (192 ticks), matching the proven earlier implementation.
  Delayed acknowledgements can now rewind and replay instead of forcing a history-gap reset.
- Added focused tests for immediate shot presentation, receipt correction, rejection and
  timeout cleanup, authoritative handoff, protocol validation, server correlation, and a
  2.4-second delayed vehicle acknowledgement.

## 17:22 AST - Interaction-island v2 protocol checkpoint

- Preserved the removed interaction-island implementation at commit
  `5c899fda601357f07c96dbc47855edd96d36e95e` as a behavior and soak-test reference; the
  forward port remains isolated on `codex/interaction-islands-v2`.
- Added interaction protocol v7 with same-tick body, remote-intent, and contact baselines.
  Snapshots carry collision, streaming, surface, control, shape, and lifecycle revisions and
  fail closed across geometry, ownership, or elevation changes.
- Added four ordered rollout stages: snapshot transport, selection, vehicle-island replay,
  and mixed-island replay. Every new stage defaults off, so the existing local predictors and
  remote timelines remain the production fallback.
- Documented the compatibility boundary and retained policy baseline: 24 history ticks,
  32 desktop points, 20 mobile points, and weighted vehicle/humanoid/prop costs.
- Added protocol, dependency, configuration, and fail-closed tests before integrating any
  snapshot publisher or replay runtime.

## 18:10 AST - Authoritative interaction snapshot projection

- Added immutable post-step Rapier body/contact frames instead of deriving physical baselines
  from delayed Colyseus schema state. Frames are captured only after every elevation surface
  world settles for the fixed simulation tick.
- Extended the persistent body registry with stable actor metadata and monotonic shape and
  lifecycle revisions. Surface migration, collider replacement, removal, and recreation can
  now invalidate replay history explicitly rather than relying on positional heuristics.
- Added the v2 server projector with contact-first admission, finite time-to-contact ranking,
  weighted budgets, strict same-space/same-surface filtering, applied remote intents, control
  revisions, canonical contact deduplication, outgoing protocol validation, and bounded
  authoritative history.
- Wired snapshot transport into the district patch lifecycle behind the disabled-by-default
  `GAME_NETCODE_INTERACTION_SNAPSHOTS` rollout flag. Existing predictors and remote timelines
  remain untouched when the flag is off.
- Added focused registry and projector coverage. TypeScript and 15 protocol, rollout, registry,
  and projection tests pass.

## 20:26 AST - Client interaction admission and selection

- Added a fail-closed client inbox for protocol v7 snapshots with collision-revision checks,
  bounded sorted history, duplicate-tick replacement, future-window rejection, and diagnostics.
- Added deterministic weighted island selection with root-first admission, explicit-contact
  priority, six-tick contact hysteresis, server-ranked fallback order, desktop/mobile budgets,
  overflow reporting, and revision-driven membership resets.
- Kept the milestone observational behind the disabled `interactionSnapshots` and
  `interactionSelection` rollout stages. It does not replay remote actors or alter movement,
  collision, combat, or presentation.
- Added a modular controller and complete debug-panel row for bodies, points/budget, overflow,
  contacts, retained contacts, snapshot age, history, server tick, and resets.
- TypeScript and seven focused client admission, selection, coordination, and shell-contract
  tests pass.
