# 2026-07-30

## Layered Bridge Underpasses

- Traced the blocked underpass near world position `(7600, 10008)` to the GTA2 source map. The road approaches contain a lower `z=2` surface, while the bridge span stores only its elevated `z=3` road lid.
- Updated the OpenGTA2 web exporter to synthesize a lower surface only for short, bounded gaps covered entirely by an elevated road. Ordinary buildings and unbounded empty areas do not qualify.
- Preserved the authored elevated bridge deck as a separate traversable surface, allowing actors on the bridge and actors beneath it to occupy the same `x/y` coordinates.
- Added a generated-map regression proving both sheets exist and that a vehicle can traverse the lower road across the complete bridge span.
- Added the development QA route `/?renderer=three&qa=underpass`, which spawns on the lower road immediately east of the bridge.
