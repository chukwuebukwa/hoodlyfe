# 2026-08-22

## 07:10 AST - Interaction-island v2 vehicle replay observation

- Continued the isolated forward port on `codex/interaction-islands-v2` after the protocol,
  authoritative snapshot projector, and deterministic client selection checkpoints at
  `7f9ddf4`, `1e9b16c`, and `119c455`.
- Added immutable, contiguous saved vehicle inputs to the current prediction controller and
  the historical two-tick hold/four-tick decay policy for remote vehicle intent.
- Added a separate Rapier vehicle-island replay world. It restores the same-tick authoritative
  selected vehicles, advances the local root from saved inputs, advances peers from continued
  server-applied intent, resolves vehicle contacts together, and rejects unsupported mixed
  bodies, sequence gaps, oversized history, invalid shapes, and failed captures.
- Replayed movement checks current elevation surfaces and occupancy through `SurfaceMap`; the
  historical flat-world collision kernel was not restored.
- Wired replay behind the disabled-by-default `vehicleIslandReplay` rollout stage. The result is
  observational only: it records root correction, angular divergence, contacts, replay ticks,
  selected vehicle count, and surface rejects without replacing the live vehicle presentation
  or mutating authoritative state.
- Extended the F3 interaction-island row with replay state and diagnostics. Existing local
  vehicle prediction remains the active production fallback while replay is disabled or fails
  closed.
- QA: TypeScript passes, focused replay/controller coverage passes 14/14, the permanent netcode
  suite passes 95/95, and the production build completes. The full repository suite passes
  801/805; its four population, pedestrian, and traffic-soak failures reproduce identically in
  the untouched `main` worktree and are not regressions from this milestone. Activation remains
  blocked on a current-Rapier impairment soak, correction thresholds, elevation transitions,
  and browser staging evidence.

Next: port the historical latency/jitter/retransmission soak to the current Rapier and surface
architecture, establish activation thresholds, then canary presentation takeover separately
from mixed vehicle/humanoid replay.

## 08:16 AST - Vehicle-island replay impairment gate

- Replaced the stale 24-tick interaction history constant with an explicit 800 ms contract
  derived from the shared 60 Hz simulation step. The resulting 48-tick window preserves the
  original design duration instead of silently shrinking history to 400 ms after the tick-rate
  change.
- Added a deterministic current-Rapier vehicle-island soak using the production protocol
  validator, mobile 20-point selector budget, replay controller, shared vehicle drive/capture
  kernel, contact resolution, saved local input, remote intent continuation, and final
  authoritative convergence.
- Added two permanent impairment profiles: 150 ms RTT / 30 ms jitter / 1% loss and 250 ms RTT /
  45 ms jitter / 2% loss. Each run covers eight authoritative vehicles, four simultaneous
  client roots, contact-first selection, budget overflow, delayed snapshots, and simulated
  reliable retransmission. Every profile is rerun with the same seed and its deterministic
  replay trace must match exactly.
- Continental evidence: 892 accepted snapshots, zero rejected, 12 retransmissions, 15 maximum
  replay ticks, five selected vehicles / 20 weighted points, 892 overflow selections, 888
  snapshots with contacts, zero budget violations, 0.251 ms p95 replay cost, and zero final
  convergence error.
- Intercontinental evidence: 886 accepted snapshots, zero rejected, 18 retransmissions, 26
  maximum replay ticks, five selected vehicles / 20 weighted points, 886 overflow selections,
  882 snapshots with contacts, zero budget violations, 0.342 ms p95 replay cost, and zero final
  convergence error.
- Updated inbox retention coverage to derive eviction from the shared history window rather
  than assuming a hard-coded tick count.
- QA: focused interaction/replay coverage passes 8/8, the permanent netcode suite passes 98/98,
  TypeScript passes, and the optimized Next.js production build completes.
- Scope boundary: this is a vehicle-only activation gate with exact shared-model parity. Its
  zero correction error proves deterministic replay and convergence under delayed delivery; it
  does not yet model a deliberately divergent client predictor, mixed humanoid/vehicle replay,
  elevation transitions during replay, or presentation takeover. Runtime behavior therefore
  remains observation-only and disabled by default.

Next: add controlled predictor divergence and elevation-transition cases, then define the
vehicle-only canary takeover gate before extending the replay island to humanoids and combat.
