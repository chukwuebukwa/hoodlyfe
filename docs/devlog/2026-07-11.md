# Development Log - 2026-07-11

## 00:23 - Street AOI, Population LOD, and Traffic Deadlock Recovery

### Production Study

- Rechecked pinned re3 lane-link, lane-offset, traffic-light, obstacle-speed, temporary-action, and offscreen cleanup ownership before changing traffic behavior.
- Recorded the clean-room streaming and traffic contract in `STREET_STREAMING_TRAFFIC_RESEARCH.md`.

### Architecture

- Added per-client street AOI with 1,280/1,536-pixel hysteresis, deterministic priority, bounded add/remove work, same-space global facts, and occupied/mission vehicle pinning.
- Added 80 pedestrian and 64 traffic lightweight population records with 1,536/1,920-pixel activation hysteresis, bounded transitions, coarse dormant movement, 40/24 active ceilings, and combat/damage/occupancy/mission pins.
- Removed the live room's duplicate 16-car bootstrap producer; persistent parked/service vehicles remain separate from streamed ambient traffic.
- Added opposite right-hand compatibility lane offsets, deterministic expiring junction reservations, road-safe reverse/replan, stationary-car reverse/pass/merge, direct pedestrian detours, and ambient pedestrian road avoidance.
- Replaced circular vehicle-to-vehicle collision with catalog-authored oriented rectangles, bounding-circle broad phase, SAT minimum-axis separation, and existing momentum/damage response.
- Fixed the Three debug adapter so F3 displays street AOI and population pressure instead of incorrectly reporting both as off.

### QA

- Added policy, population, lane, junction, maneuver, pedestrian detour, collider geometry, and one-minute 24-car circulation soak coverage.
- Production TypeScript/Vite build passed. Focused traffic/collision tests passed. Full suite reached `213/214`; the only miss was the long combat integration scenario's known intermittent participant-death timing, and its immediate isolated rerun passed.
- Live Three QA showed 27 total vehicles, confirming 24 streamed traffic plus three persistent vehicles rather than the accidental 40-car composition. F3 reported `29 avg / 89 world` AOI pressure and `64/144 / 21 pinned` population pressure with zero dropped simulation time.

### Next Analysis

The compatibility-road solution now bounds and recovers ordinary conflicts, but dense production traffic still needs authored directed lane centerlines, legal turn connectors, segment capacity, merge priority, parking links, and vehicle-class restrictions. Those should be content data consumed by the existing traffic owners, not new room logic.

## 01:15 - Authored Storefront Interiors and Permanent Map Locations

### Architecture

- Added authored single-floor Ammu-Nation, Threads, and Southside Clinic definitions with exact roof-triangle contracts, facade doors, collision bounds, fixtures, service anchors, and clinic recovery ownership.
- Moved ammunition, clothing, and Southside medical services into their matching spaces. Street state views now retain only the repair garage while each interior receives its own same-space service.
- Added permanent street storefront minimap points independent of indoor service replication, plus dedicated 64-pixel ammunition, clothing, hospital, and repair icons with procedural fallbacks.
- Generalized the Three interior renderer around catalog obstacles and kind palettes instead of hardcoded Mercy coordinates.
- Preserved the original minimap icon atlas under `art/source/`; only the four runtime crops ship from `public/`.
- Added the three named roof groups to the OpenGTA2 exporter in sibling commit `cb6607c`.

### QA

- Tested the exact staged Git tree in isolation so active character-pipeline edits could not mask missing dependencies.
- Staged production build passed; `30/30` focused and real two-client tests passed, including all four roof contracts, service isolation, map markers, medical ownership, pedestrian schema, and the street/interior round trip.
- Live Three reload remained nonblank and exposed 11 current minimap markers with no fresh browser errors. The old Colyseus errors in retained browser history predate this checkpoint.

## 01:24 - Authoritative Player-Death Cash Drops

### Production Study

- Rechecked the pinned Vice City pickup controller for its separation of world lifetime, availability, proximity eligibility, and inventory mutation.
- Kept NOCK0's rule original and zero-sum: the victim is debited before a recoverable world pickup can exist.

### Architecture

- Added a modular `CashPickupController` with a bounded 48-drop lifecycle, one-second collection delay, 60-second expiry, deterministic contention, and on-foot/street/alive/idle eligibility.
- Added 20%-of-balance death loss with a $50 threshold and $500 cap, using stable idempotency keys for both debit and collection.
- Added `cash-pickup.collected` diagnostics, AOI-hysteretic replication, Phaser and Three world markers, and nearby minimap markers.
- Kept all cash mutation behind `StreetEconomyController`; combat, room orchestration, replication, and renderers do not mutate balances.

### QA

- Focused cash/economy/replication/minimap tests pass, covering duplicate death events, deterministic contention, delay, expiry, capacity, full balances, and invalid collection states.
- Exact staged-tree TypeScript/Vite production build passed and all `212/212` staged tests passed, including the real two-client scenario.
- Live Three QA loaded a nonblank 716x803 game canvas with 4 players, 18 nearby NPCs, and 25 nearby vehicles. No fresh browser errors appeared; retained Colyseus errors are timestamped before this checkpoint.

## 01:40 - Character Compiler Prototype and Rejected Paint-Over Direction

### Goal

- Build a standalone character-production tool that compiles one appearance recipe into aligned idle, walk, melee, hit, fall, vehicle-entry, carjacking, and death sheets for the canonical Three renderer.
- Replace one-off runtime sprite edits with versioned animation contracts, data-driven parts, compatible body families, semantic material channels, and bounded compiled-texture caching.

### Implemented Prototype

- Added the standalone Character Lab at `/creator.html` with animation/frame inspection, part and material controls, randomize/reset, local recipe storage, and JSON/walk/action exports.
- Added a versioned character animation manifest, part catalog, compiler, material masks, authored-layer composition hook, semantic action selection, and a 96-appearance Three texture cache.
- Removed the old in-game creator modal and routed Three player walk/action source loading through the shared character manifest instead of hardcoded asset paths.
- Added an AI-authored 3x3 idle/walk source atlas and normalized its dimensions and scale against the action atlas.
- Removed `drawAppearanceStyleDetails` from the new compiler path so its crude rectangle-based hair, hat, jacket, pants, and shoe stand-ins no longer execute there.
- Bumped the character asset contract to version 3 and cache-busted the new walk/action sources and masks.

### What Failed

- The visual result was rejected. The idle/walk output still read as a paint-over rather than a designed modular character: an oversized recolored hair mass, coarse top/pants/shoe blocks, incorrectly classified glove/arm pixels, and fake width scaling for the un-authored `broad` body.
- Removing the rectangle painter did not solve the underlying problem because coarse material masks still flooded regions of a single finished sprite with arbitrary colors. This is not equivalent to authored body, hair, shirt, pants, shoe, and accessory layers.
- Part controls continued to advertise hairstyles, clothing, headwear, and body variants that had no authored per-frame silhouettes. The metadata and compositor extension points existed, but the displayed content did not honestly satisfy those contracts.
- Technical correctness was mistaken for visual completion. The creator loaded and animated without browser errors, but that did not make the produced character acceptable.
- The attempted AI walk atlas was more internally consistent than the legacy GTA2 base, but its top-down proportions and material segmentation still did not match the required art direction. It remains a prototype asset, not approved production art.

### QA Results and Blockers

- The focused character manifest, material-channel, cache-key, and PNG-dimension suite passed `6/6`.
- Browser QA confirmed the standalone creator and canonical Three renderer loaded nonblank with no fresh console errors. Idle, walk, and melee clips switched correctly and the walk/action size pop was reduced.
- Full repository tests reached `226/227`. The failing existing long multiplayer integration scenario reported `Melee participants must remain alive (player=false/0, npc=true/60)`; this failure was outside the character compiler slice.
- Repository TypeScript and production build were blocked by an unrelated active change in `server/game/debug/debug-snapshot-controller.ts`: `TS2366`, function lacks an ending return statement. That shared work was not modified in this side task.
- No character milestone commit was created. The creator, compiler, documentation, tests, and generated assets remain part of the dirty working tree alongside unrelated active changes.

### Universal LPC Evaluation Started

- Began evaluating the Universal LPC Spritesheet Character Generator as a replacement reference for the failed paint-over approach.
- Confirmed its useful production patterns: synchronized transparent layers, metadata-driven categories and z-order, body compatibility, per-animation item coverage, palette definitions, JSON import/export, attribution export, split-sheet export, and a large existing clothing library.
- Confirmed important constraints: the generator code is GPL-3.0; individual art has mixed CC0, CC-BY, OGA-BY, CC-BY-SA, and GPL terms; selected authors and licenses must be tracked and exposed in game credits, and derivative/share-alike obligations vary by asset.
- LPC's standard 64x64 four-direction RPG perspective and animation layout do not directly match NOCK0's rotating top-down GTA presentation or current 72x72 semantic atlases. Its architecture is highly relevant, but adopting its art unchanged would produce a visible perspective mismatch and require renderer/animation conversion work.
- No LPC source, generator code, or art was copied into NOCK0 during this evaluation. The deeper adopt/adapt/reject decision was moved to the main task.

### Required Next Decision

- Stop extending mask-based paint-over customization.
- Decide in the main task whether to: use LPC as architecture only and author a NOCK0-specific top-down layer library; fork/convert a license-filtered LPC subset; or adopt LPC's visual perspective and replace the current character presentation contract.
- Whichever direction is selected, expose only genuinely authored options in the creator. A selectable item must provide aligned frames for every required clip, explicit layer order and occlusion, body compatibility, palette ramps or authored colors, and attribution metadata where required.

## 04:44 - LPC Character Creator Integration Guide

### Documentation

- Added `docs/LPC_CHARACTER_CREATOR_GUIDE.md` as the durable implementation guide for the current LPC character creator.
- Documented the core storage decision: persist LPC recipe JSON/hash, not generated sprite PNGs.
- Documented the runtime flow from Threads wardrobe open, through LPC modal editing, through `appearance.update`, Colyseus replication, client-side LPC compilation, local reload storage, and future durable account persistence.
- Captured the relevant ownership boundaries: shared recipe catalog, catalog importer, standalone creator lab, in-game creator controller, LPC sprite compiler, and Three player renderer.

### Custom Asset Pipeline

- Recorded how custom LPC-compatible assets should be added through `scripts/import-lpc-catalog.py`.
- Captured the current generated custom items: `Smiley Tee`, `Timbs`, and `Yarmulke`.
- Documented the important yarmulke fix: small headwear must derive placement from per-frame head alpha bounds so it follows walk/animation bobbing instead of floating at a static coordinate.
- Documented fixed-color material behavior: fixed custom tops/shoes/hats disable their corresponding color swatches, while colorable LPC assets continue using variant files.

### Threads/In-Game Creator Notes

- Recorded the middle-ground integration target: keep `/creator.html` as the development lab while Threads opens an LPC-backed in-game creator modal.
- Documented that the Three route has its own UI/input path and therefore wires the LPC creator through `ThreeDistrictUiController` for input blocking and wardrobe-open handling.
- Listed focused verification commands for future changes:
  - `python3 scripts/import-lpc-catalog.py`
  - `npx tsx --test test/appearance-catalog.test.ts test/player-appearance-controller.test.ts test/wardrobe-client-session.test.ts`
  - `npm run build`

## 01:58 - Most Wanted Eliminate-Target Contract

### Production Study

- Rechecked pinned Vice City script objective assignment, pedestrian kill-objective execution, death completion, player threat queries, and mission cleanup ownership.
- Recorded the clean-room boundary and exact references in `MOST_WANTED_RESEARCH.md`.

### Architecture

- Added the fifth Freemode template, `Most Wanted`, with a reusable `eliminate-target` objective and explicit `eliminate` phase.
- Extended mission encounter content with validated `guard` and single-count `target` roles plus bounded living-roster scaling at wave start.
- Added two guards plus one per extra living crew member, followed by one stable 220-health SMG boss with faster fire cadence.
- Replicated `targetNpcId` through mission state, pinned the target outside ordinary AOI for participants only, and projected the same actor through Phaser/Three world markers, minimap, and HUD.
- Preserved combat ownership in pedestrian navigation/fire/melee/projectile/damage systems and cleanup ownership in the mission entity scope.

### QA

- Added catalog, role validation, roster scaling, objective, mission-system, room-adapter, presentation, payout, cleanup, and participant-only replication coverage.
- Exact staged-tree production build and all `219/219` staged tests passed, including the real two-client scenario.
- Live Three QA retained `Most Wanted` after selector updates, showed the `$1500` offer and guarded-hideout summary on a nonblank 1280x720 canvas with three players, and produced no fresh console errors.

## 02:34 - Authoritative Rocket Launcher

### Production Study

- Rechecked the pinned Vice City rocket lifecycle at `3233ffe1`: launch-origin validation, moving missile ownership, 1.4-second lifetime, smoke, collision/line-of-sight termination, and typed explosion dispatch.
- Recorded the clean-room translation and deliberate browser-game boundaries in `ROCKET_LAUNCHER_RESEARCH.md`.

### Architecture

- Added `rocket` as a distinct shared weapon fire mode with four-round capacity, accepted-launch ammo consumption, 950 ms cadence, no vehicle-seat firing, and shared HUD/held-model metadata.
- Added a modular authoritative `RocketProjectileController` with two-per-owner/32-global limits, stepped world collision, earliest swept circle impact across players/NPCs/vehicles, 1.4-second expiry, and bounded duplicate-detonation tombstones.
- Extended the existing explosion authority with a typed rocket blast instead of duplicating radial damage, attribution, reactions, vehicle component damage, or chain reactions.
- Added AOI replication plus original Phaser/Three launcher and in-flight presentation. Ammu-Nation now prices and restores missing rocket ammunition.

### QA

- Added focused catalog, fire gate, ammo, passenger restriction, capacity, sweep, wall tunneling, expiry, duplicate detonation, blast attribution/damage, service price, held-model, and explosion-presentation coverage.
- Focused rocket/presentation tests and the real two-client scenario passed. The exact staged-tree production build and all `225/225` staged tests passed with concurrent character/audio work excluded.
- Live Three QA showed the original launcher model, `ROCKET LAUNCHER` HUD, four-round count, nonblank 2560x1440 canvas, and no fresh browser errors.

## 04:47 - Authoritative Day/Night Lighting and LPC Character Pipeline

### World Time and Lighting

- Added one server-authoritative replicated world-clock anchor with a 48-real-minute full day. Clients derive display time locally instead of receiving per-tick clock replication.
- Added continuous night, dawn, day, and dusk lighting policy for sky, fog, hemisphere, ambient, sun color/intensity, and streetlight intensity.
- Added development time controls to the existing DBG panel plus `?time=night|dawn|noon|dusk|0..23` launch overrides.
- Replaced unlit Three map materials with Lambert materials and computed geometry normals so world lighting affects roofs, walls, and streets coherently.
- Added invisible warm road emitters, authored gantry anchors, and replicated red/yellow/green traffic-signal glow without fake visible lamp geometry.
- Replaced sparse edge-only placement with deterministic road-mask coverage. The current district generates 247 procedural emitters plus 16 authored fixtures and covers all 1,861 road tiles within a 168-world-unit radius; only the nearest 16 are active per client.

### LPC Character Pipeline

- Imported a generated LPC catalog with synchronized body, hair, hat, torso, leg, and footwear animation layers plus manifest metadata.
- Added stable shared LPC recipe parsing/validation, server replication, creator storage, client-side compilation, and bounded runtime texture integration.
- Added the in-game Threads creator flow and standalone creator lab for genuinely authored LPC choices instead of advertising paint-over options without matching silhouettes.
- Added catalog import tooling and documented custom LPC content authoring, fixed-color behavior, layering, runtime storage, and licensing/attribution constraints.

### QA and Checkpoint

- Production build passed.
- Full repository suite passed `252/252` on the final rerun.
- Browser QA at forced night showed a nonblank Three scene, continuous road illumination, signal glow, live debug time controls, and no fresh console errors.
- Committed and pushed as `74d8c47` (`Add procedural night lighting and LPC characters`).

## 05:02 - Vehicle Night-Light Foundation

### Production Study

- Rechecked pinned Vice City automobile lighting for time/engine gates, model anchors, separate front/rear damage, brake/reverse differentiation, camera-distance culling, cheap coronas, and player-only real light casts.
- Recorded the clean-room translation and deliberate omissions in `VEHICLE_NIGHT_LIGHTING_RESEARCH.md`.

### Implementation

- Added a renderer-neutral vehicle-light presentation policy driven only by replicated vehicle state and the shared day/night intensity.
- Added damage-scaled warm front glow, red rear glow, and white reverse glow to the Three vehicle presentation.
- Bounded active vehicle glow presentation to the ten nearest replicated cars and disabled it for daylight, interiors, destroyed/burning vehicles, and abandoned non-traffic cars.
- Kept braking out until a stable braking fact exists; client speed-delta inference would be nondeterministic visual noise.

### QA

- Production build and focused vehicle-light/world-light tests passed `8/8`.
- Live forced-night Three scene reconnected nonblank with no fresh browser errors, but no traffic entered the current camera frame; live lamp orientation remains an explicit visual acceptance check before this foundation is promoted.

## 05:24 - Emergency Siren Yielding

### Production Study

- Rechecked pinned Vice City `MakeWayForCarWithSiren`: moving-siren gating, speed-scaled forward projection, random-civilian filtering, emergency exclusion, aligned temporary swerves, conflicting-traffic waits, and timed action expiry.
- Recorded the clean-room translation and ownership boundaries in `SIREN_YIELD_RESEARCH.md`.

### Architecture and Behavior

- Added modular `EmergencyYieldSystem` below police strategy and above shared road driving.
- Nearby active sirens now create deterministic per-car `yield-left`, `yield-right`, or `wait` actions lasting 1.8 seconds.
- Strongly aligned traffic probes the side away from the siren corridor, falls back to the opposite road-safe side, and waits if neither is valid. Oncoming and cross traffic wait instead of swerving through intersections.
- Silent, stopped, destroyed, behind, distant, and out-of-corridor emergency vehicles are ignored.
- Normal road steering/braking/collision remains in `RoadDrivingSystem`; blocked-route recovery is suspended while yielding so a legitimate emergency action is not treated as a traffic jam.
- Extended traffic diagnostics with `siren`, yield phase, and emergency ID. Phaser debug text and Three F3 links expose active relationships.

### QA

- Focused emergency policy, traffic controller, maneuver, police cruiser, debug snapshot, and one-minute circulation soak tests pass.
- Production build passes.
- Live Three DBG QA loaded one player, 51 NPCs, 22 vehicles, expanded diagnostics, visible night vehicle glows, and no fresh browser errors. No pursuit was active in that snapshot, so live siren-link presentation remains secondary to the deterministic controller/diagnostic coverage.
- Full parallel testing exposed a pre-existing contradiction in the long multiplayer scenario: Holdout permits individual death, but its later melee helper asserted that the player could never die while approaching a hostile. Under test-runner load the independent server continued firing while client callbacks lagged.
- Updated the scenario to wait through the normal medical respawn, return through the ordinary interior doorway flow, and resume the melee approach. Increased the scenario timeout to cover that legitimate recovery without introducing a server teleport or test-only gameplay command.
- The isolated two-client scenario passed, then the complete parallel repository suite passed `258/258` in 28.4 seconds.

## 05:38 - Three Emergency-Light Parity

### Implementation

- Confirmed Phaser already owned alternating police lamps and both clients already used the shared positional `VehicleAudioSystem` siren loop.
- Added a pure emergency-light presentation policy and Three red/blue roof glows on the existing 120 ms alternation cadence.
- Gated the effect to operable police cruisers with an active replicated siren; silent, burning, destroyed, and non-police vehicles remain dark.
- Reused existing vehicle teardown and shader disposal paths, adding no server state or network messages.

### QA

- Production build and 12 focused vehicle-light, emergency-yield, and traffic-controller tests passed.
- Live forced-night client reloaded without errors and ordinary firing exercised combat, death, cash reward, Mercy respawn, and roof removal. The player died before an active cruiser remained in-frame, so live red/blue pursuit capture remains an explicit visual follow-up rather than an unverified completion claim.

## 12:50 - Molotov Ground Fire and Vehicle Lamp Orientation

### Production Study and Architecture

- Rechecked pinned Vice City projectile, Molotov impact, explosion, and fire-manager boundaries. Recorded the clean-room translation and explicit follow-ups in `MOLOTOV_FIRE_RESEARCH.md`.
- Added Molotov content to the shared weapon catalog with separate ammunition, cooldown, model, projectile tuning, and Ammu-Nation pricing/restock support.
- Generalized `ThrownProjectileController` by projectile kind while preserving grenade bounce/fuse behavior. Molotovs shatter on world or descending ground impact and resolve through a dedicated `FireZoneController`, not the one-shot explosion controller.
- Added bounded authoritative ground-fire lifetime, owner/global capacity, source attribution, 500 ms damage cadence, direct-player/vehicle double-hit prevention, AOI replication, positional ignition audio, and sustained pedestrian fire stimulus.
- Added original bottle artwork plus replicated Phaser and Three flight/fire presentation.

### Vehicle Lamp Correction

- Diagnosed headlights spawning beside cars because world lamp anchors reused the vehicle sprite rotation, which contains an atlas-specific negative quarter-turn.
- Added a pure physical-heading lamp-anchor policy and switched front/rear Three glows to it while leaving the sprite correction intact.
- Added cardinal east/south/west/north anchor regression coverage.

### QA

- Production build passed.
- Focused vehicle-light and cardinal-anchor tests passed `7/7`.
- Full repository suite passed `263/263`, including the two-client weapon-cycle scenario with the new Molotov slot.
- Live forced-night Three client reloaded nonblank with no console errors. Its current camera frame contained no traffic vehicle, so lamp placement is covered by deterministic heading tests and still merits an in-frame visual confirmation during the next traffic pass.

## 13:12 - Catalog-Driven Street Weapon Pickups

### Architecture

- Replaced the grenade-specific pickup controller branches with immutable shared pickup definitions and the existing shared weapon/ammunition accessors.
- Added a separate deterministic Molotov cache with its own quantity, capacity, placement seed, and respawn policy.
- Kept contention, proximity, lifecycle, events, notices, AOI replication, and minimap ownership in the existing modular pickup boundary.
- Updated Phaser and Three pickup presentation to select the replicated weapon model and label rather than assuming every pickup is a grenade.

### QA

- Production build passed.
- Focused catalog, pickup authority, rendering-policy, and minimap coverage passed `19/19`.
- Added explicit Molotov capacity, partial grant, notice, event identity, and respawn assertions while retaining deterministic grenade contention coverage.

## 13:24 - Vehicle Lamp Interpolation Follow-up

- User QA showed the first physical-heading correction still separated from the rendered car during turns because the sprite eases toward replicated rotation while the lamp snapped immediately.
- Added a rendered-sprite lamp anchor that removes only the atlas quarter-turn after interpolation, keeping front and rear glows attached to the visible car throughout steering corrections.
- Tightened the front glow footprint and moved its center beyond the bumper so it projects forward instead of covering half the car.
- Production build and eight focused heading/light tests passed. A fresh forced-night Three client showed an angled teal traffic car with its warm glow extending from the visible front bumper, and no console errors.
- A second user capture exposed remaining positional drift on moving cars: the sprite position was interpolated but lamp `x/y` still used the newest replicated coordinates. Front/rear lamps now consume the rendered mesh position and rotation as one transform, eliminating translation and steering divergence together. The tightened front footprint remains bumper-forward.

## 14:42 - Finite Carried Pedestrian Burns

### Architecture

- Added a dedicated `ActorBurnController` below ground-fire contact and above the existing damage/lifecycle owners.
- Ground fire now ignites exposed on-foot players and NPCs through a narrow burn port; occupied players remain excluded so the vehicle component-fire path is not duplicated.
- Carried burns preserve source attribution, extend while an actor remains in ground fire, apply damage every 750 ms, continue for 3.5 seconds after last contact, and clear on death, respawn, expiry, or entity removal.
- Added replicated actor fire flags and one renderer-neutral flame pulse policy consumed by Phaser and Three. Effects follow interpolated actor transforms and remain presentation-only.

### QA

- Production build passed after authority and presentation integration.
- Focused ground-fire, carried-burn, and presentation tests passed `16/16`, covering cadence, extension, attribution, occupant exclusion, death cleanup, state gating, and local animation.
- Full repository suite passed `268/268`.
- Fresh forced-night Three client loaded the new Molotov cache and no console errors. The desktop automation pointer press was too brief for the frame-driven fire gate, so carried-flame visual capture remains an explicit manual QA item rather than a claimed pass.

## 15:26 - Heat-Scaled Police Response Fleet

### Production Study and Architecture

- Rechecked pinned Vice City `Wanted.cpp`, `CarCtrl.cpp`, and the police blocking path. The production pattern separates wanted-level vehicle budgets and delayed, clear, reachable spawning from each cruiser's driving mission.
- Added `PoliceResponseFleetController` as the sole owner of dynamic response population. `DistrictRoom` only constructs it, provides index callbacks, exposes diagnostics, and calls one fixed-step update.
- Heat now requests one, two, or three available cruisers. The authored parked cruiser satisfies the budget first; dynamic deficits arrive one at a time with higher-heat cadence.
- Candidate response cars must occupy road space, stay clear of all vehicles and street players, fall within a bounded response annulus, and complete a bounded road-plan to the highest-heat suspect.
- Dynamic cruisers use the existing police assignment/search/pursuit driver. Hijacked or destroyed cars leave fleet and police ownership without being deleted, allowing the normal player/wreck lifecycle to continue.
- Surplus cars stand down after 7.5 seconds and are removed only when fleet-managed, healthy, unoccupied, and at least 520 world units from every street player. Authored and stolen vehicles are excluded.
- Added fleet diagnostics for desired, available, managed, and next-spawn values plus `POLICE_RESPONSE_FLEET_RESEARCH.md`.

### QA

- Added policy, heat scaling, spawn clearance/separation, authored-unit reuse, hijack replacement, and delayed stand-down tests.
- Full repository suite passed `273/273`; production TypeScript and Vite build passed.
- Fresh forced-night Three client reloaded after the server change with no console errors and a nonblank live world.
- Rechecked the final vehicle-lamp correction live: front glow remains centered beyond the visible bumper because position and rotation now both follow the interpolated rendered car transform.

## 17:01 - Production Network Telemetry and Local Driver Prediction

### Infrastructure Boundary

- Added a production multiplayer infrastructure plan covering US/Asia regional topology, party-wide region scoring, room authority, Colyseus/Redis routing, durable identity and economy boundaries, reconnect behavior, capacity tests, SLOs, and staged rollout gates.
- Extended `/health` with region, replica, build, uptime, RSS, and event-loop delay percentiles so geographic latency can be separated from process pressure.
- Added a rate-limited application ping/pong protocol and an F3 network panel for region/build identity, RTT p50/p95, jitter, patch-gap p95, server tick, prediction error, and hard reconciliation count.
- Added monotonic movement input sequences and replicated acknowledgements. Stale, duplicate, and implausibly far-ahead commands fail closed while legacy test callers remain compatible.

### Client Responsiveness

- Added one catalog-driven vehicle prediction policy shared by Phaser and Three. It mirrors authoritative forward/reverse acceleration, opposite-throttle braking, coast deceleration, speed limits, steering grip, and reverse steering.
- Added frame-rate-independent reconciliation with bounded correction and a hard snap only past an unsafe 180-pixel divergence.
- Wired the Three camera, player/passenger presentation, name labels, and vehicle effects to the predicted render pose. Input is sampled before the render prediction step instead of after the frame.
- Preserved the concurrent onboarding/player-presence gating and character pipeline changes while integrating the network layer.

### QA

- Production TypeScript and Vite build passed.
- Full repository suite passed `280/280`, including new probe abuse, input ordering/acknowledgement, telemetry projection, RTT/patch sampling, and vehicle prediction/reconciliation coverage.
- Live Three.js browser QA joined a local district, rendered nonblank, and produced no console errors.
- F3 reported `local / development`, RTT p50/p95 around `2.5/4.5 ms`, patch p95 around `52 ms`, live server ticks, and prediction correction counters.
- Local health reported event-loop p95 around `12 ms`; its historical max exceeded one second, validating the plan to alert on event-loop/tick pressure rather than CPU alone.

### Remaining Gates

- Add replay-based on-foot prediction and timestamped interpolation buffers for remote actors.
- Add clock synchronization, reconnect tokens, duplicate-session policy, and bounded combat lag compensation.
- Only then deploy explicit US West, US East, and Singapore room services behind measured region discovery and signed reservations.
