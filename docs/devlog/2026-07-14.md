# Development Log - July 14, 2026

## 00:52 CST - M10c Predicted Projectile Presentation and Authority Handoff

- Added a renderer-neutral combat-fire prediction controller that emits the versioned,
  monotonic fire command and creates presentation-only projectile shadows on the same
  rendered frame. Static-world stepping uses the same four-pixel spatial granularity as
  authority without applying local damage or any other gameplay side effect.
- Enriched accepted projectile receipts with authoritative weapon and pose data. Delayed
  receipts correct random spread and catch-up position, while correlation IDs remove the
  local shadow before the replicated authoritative projectile is presented.
- Rejected, resolved, malformed, and timed-out commands retire predicted presentation;
  shotgun commands preserve exact pellet cardinality and fail closed on bad mappings.
- Integrated the controller with Phaser and Three, including local recoil, muzzle action,
  aim-origin attachment ownership, predicted projectile render pools, teardown, and a
  queued Three input edge so a quick click cannot disappear between render samples.
- Preserved the legacy interior fire path because interior historical collision is not
  yet part of M10; street combat exclusively uses the new validated command path.
- Catch-up resolution now leaves a consumed projectile at its exact historical actor or
  static-wall impact pose in the receipt, rather than reporting its pre-impact step pose.

QA: focused M10 tests pass, the permanent netcode gate passes 110/110, the complete
repository suite passes 403/403, and the real two-client Colyseus scenario passes in
22.8 seconds. TypeScript, `git diff --check`, and the optimized production build pass.
Live desktop Phaser, desktop Three, and 390 x 844 mobile Phaser sessions connected,
rendered nonblank worlds, and consumed pistol ammunition on click. No new M10c runtime
errors appeared; known unrelated Privy, Three.Clock, and audio listener warnings remain.

## 01:25 CST - Interaction-Island Debug Surface Completion

- Split the previous compressed interaction-island sentence into an island budget row
  and an island replay row. The panel now exposes bodies, weighted points and budget,
  overflow bodies and points, dynamic horizon, snapshot age, retained history, replay
  count and ticks, replay p95, pair steps, suppressed effects, and hard resets.
- Kept the ordered island selection row for root/member IDs, admission reasons, TTC, and
  overflow. Expanded the legend to the exact root, current contact, retained contact,
  imminent contact, exit hysteresis, contact closure, overflow, and presented-pose colors
  used by both renderer overlays.
- Brought the standalone Vite shell to parity with the React shell and added a source
  contract test so either browser entry point fails the netcode gate if an island field or
  legend category disappears.

Live QA: Phaser and Three both connected to the real local district, displayed populated
budget/replay/selection rows, and drew nonblank reason-colored world overlays. At 390 x
844, the 340px panel and all three rows fit without horizontal overflow. The focused
debug tests pass 7/7, the expanded netcode gate passes 111/111, the complete repository
suite passes 404/404, TypeScript and `git diff --check` pass, and the optimized production
build passes. No new runtime errors appeared; known Privy and Three.Clock warnings remain.

## 02:56 CST - M11a Negotiated Netcode Rollout Controls

- Added a versioned, immutable rollout manifest negotiated by each client with the room
  before advanced networking stages become active. Remote timelines, interaction
  snapshots, interaction replay, combat rewind, and projectile prediction are independent
  server-owned stages with explicit dependency validation.
- Added strict startup configuration and fail-closed client fallback. Invalid flags or
  incompatible dependencies prevent a room from starting; unsupported, rejected, or
  silent servers leave the client on the shared-kernel compatibility floor instead of
  silently enabling authority-sensitive behavior.
- Wired dynamic gates through Phaser, Three, remote actor timelines, island replay,
  snapshot intake, rewound fire commands, and predicted projectile presentation. Rollback
  no longer requires a client rebuild, while current all-on behavior remains the default.
- Added a wide **Netcode rollout** debug row to both browser shells. It reports negotiation
  state, deployment revision, and the active timeline, snapshot, island, rewind, and
  projectile stages. Added a production runbook covering compatibility, dependencies,
  verification, and the safe rollback sequence.
- Added protocol, startup-config, negotiation, fallback, runtime-gating, and shell-contract
  tests to the permanent netcode gate.

Live protocol QA joined an isolated Colyseus district with revision `qa-handshake`,
negotiated all five stages, received 12 interaction snapshots in 600 ms, and observed an
advancing server tick with two connected clients. Final repository, type, build, and diff
gates are recorded after the checkpoint run below. A second isolated room negotiated the
`qa-partial` manifest with interaction replay and projectile prediction disabled while
snapshots and rewind remained active, confirming independent live rollback behavior.

QA: focused rollout coverage passes 25/25, the permanent netcode gate passes 121/121,
and the complete repository suite passes 415/415, including the real two-client Colyseus
scenario. TypeScript, `git diff --check`, and the optimized production build pass.
