# Development Log - 2026-07-10

Timezone: America/Mexico_City (CST)

## 04:01 - Deterministic Simulation and Debug Checkpoint

### Delivered

- Added a fixed-step 30 Hz simulation clock with bounded catch-up and dropped-time accounting.
- Replaced gameplay randomness with named, keyed deterministic samples.
- Added an incremental spatial index for players, NPCs, and vehicles.
- Routed bullet candidates, vehicle impacts, vehicle interaction, and nearby-police checks through the spatial broad phase while retaining exact geometry checks.
- Added deferred lifecycle commands so bullet deletion no longer mutates the collection being iterated.
- Added typed damage, death, crime, and respawn events.
- Added a shared debug snapshot protocol and periodic server snapshots.
- Added a `DBG` / `F3` overlay with collision tiles, spatial cells, entity radii, headings, IDs, health, seat/vehicle state, counts, timing, and recent gameplay events.
- Added original player, civilian, and police sprite sheets required by the active client paths.

### QA

- `npm test`: 8/8 passing, including two-client driving, passengers, shooting, death, respawn, and debug snapshot invariants.
- `npm run build`: passing production TypeScript and Vite build.
- `git diff --check`: clean.
- Live server health: HTTP 200.
- Browser QA at the default mobile-sized viewport and 1440x900 desktop.
- Live invariant observed: spatial entity count equals players + NPCs + vehicles.
- Live event feed observed: heat changes, police damage, player death, and respawn.
- Live simulation observed with zero dropped time and zero deferred commands left after the tick.

### Architecture

- Added `server/game/README.md` for simulation dependency and ownership rules.
- Added ADR 0001 for deterministic time, randomness, spatial queries, lifecycle mutations, and event contracts.
- Added `docs/RE3_AI_RESEARCH.md` with a clean-room mapping from re3 behavior to NOCK0 systems.

### Next Analysis

The next player-visible slice should build on the event boundary rather than add more direct rules to `DistrictRoom`:

1. incident registry and witness reporting;
2. per-suspect heat and last-known police search positions;
3. bounded district dispatch and pursuit assignment;
4. visible police/search diagnostics in the debug overlay;
5. a small repeatable crime-to-escape gameplay scenario.

## 04:15 - Witnessed Crime and Police Dispatch Checkpoint

### Delivered

- Added data-driven crime severities for theft, assault, hit-and-run, murder, and police variants.
- Added a bounded incident registry with deduplication, report scheduling, retention, and expiry.
- Added deterministic witness selection using NPC type, distance, hearing range, and line of sight.
- Changed wanted heat so a crime must be reported before stars appear; unwitnessed incidents expire.
- Added cumulative heat thresholds, delayed decay, and complete suspect reset on death, respawn, or disconnect.
- Added stable police dispatch assignments capped by wanted level.
- Replaced omniscient police tracking with sight-based pursuit and expiring last-known-position search.
- Added incident/report/dispatch events suitable for future audit, mission, economy, and persistence consumers.
- Extended the `DBG` / `F3` view with incident markers, pursuit/search lines, and live counters.

### QA

- Added five headless scenario tests for incident, witness, wanted, dispatch, and pursuit behavior.
- Full test suite: 13/13 passing before the final browser and integration assertions.
- Production TypeScript and Vite build: passing.
- Live browser panel verified with the upgraded debug protocol and zero dropped simulation time.

### Architecture

- Added ADR 0002: extract one domain at a time and keep `DistrictRoom` as a coordinator.
- Kept every new police module independent of Colyseus and Phaser.
- Preserved the future economy boundary: gameplay emits typed facts; persistence and onchain settlement remain asynchronous consumers.

### Next Analysis

Vehicle collision and health should be the next domain because driving, traffic, police chases, theft missions, repair shops, and cash sinks all depend on it. The slice should include vehicle-to-vehicle contacts, impact cooldowns, health, disabled/destroyed states, occupant consequences, repair, diagnostics, and deterministic tests.

## 04:24 - Vehicle Damage and Collision Checkpoint

### Delivered

- Added a standalone vehicle collision solver with mass, overlap correction, impulse transfer, and relative-speed damage.
- Added vehicle archetype configuration for health, mass, and collision damage scaling.
- Added reusable vehicle damage policy for wall impacts, weapon impacts, destruction, and health-based speed loss.
- Added authoritative car-to-car collision resolution for traffic, parked vehicles, wrecks, and player-driven cars.
- Made map-wall crashes damage vehicles instead of only reversing velocity.
- Made all weapon projectiles collide with and damage vehicles, including police fire against occupied cars.
- Added destroyed/wreck state, occupant ejection and crash injury, eight-second restoration, and traffic recovery.
- Added typed vehicle damaged, destroyed, and restored events.
- Added client smoke, fire, damage tint, wreck presentation, a driver condition meter, and health-rich debug labels.

### QA

- Full suite: 19/19 passing.
- Added pure tests for collision impulses, separating contacts, damage clamping, wall thresholds, and damaged speed.
- Added room-adapter tests proving both authoritative cars change after collision and bullets damage cars and are consumed.
- Existing two-client driving, passenger shooting, combat, police dispatch, death, and respawn scenario still passes.
- Production TypeScript and Vite build: passing.
- `git diff --check`: clean.
- Live browser debug feed observed weapon and wall damage events against `vehicle-1` with zero dropped simulation time.

### Next Analysis

The street loop now needs an information surface. Add a performant GTA Online-style minimap with the local player, nearby players, police, vehicles, and future objective/shop markers. Keep marker policy separate from rendering so parties, crews, passive visibility, interiors, and area-of-interest filtering can evolve without rewriting the HUD.

## 04:31 - Production-Reference Vehicle Damage Deepening

### Why This Replaced the First Balance Pass

The initial vehicle checkpoint correctly wired collision, weapon damage, destruction, and presentation, but its 100-point health scale was a shallow prototype. The GTA III/Vice City reverse-engineered source shows a much deeper production model: 1000 starting health, impulse and handling-based collision damage, localized body components, staged engine damage, collision-to-fire clamping, and a five-second burning window before explosion.

### Research

- Inspected `daynz/GTAviceCity` at commit `3233ffe1c4b99e8efb4c41c6794b4fce880cf503`.
- Traced vehicle initialization and weapon damage in `Vehicle.cpp`.
- Traced collision thresholds, per-part damage, fire state, particles, and explosion timing in `Automobile.cpp`.
- Traced engine, wheel, door, panel, light, bonnet, boot, bumper, and windscreen progression in `DamageManager.h/.cpp`.
- Recorded the behavior map, legal boundary, multiplayer adaptations, and calibration targets in `docs/VEHICLE_DAMAGE_RESEARCH.md`.
- Added ADR 0003 making production-reference research a required gate for every substantial gameplay domain.

### Delivered

- Raised base vehicle durability from 100 to 1000 and replicated explicit maximum health.
- Increased significant collision damage while keeping ordinary traffic contacts minor relative to the larger pool.
- Made configured weapon damage apply directly to vehicles instead of using the placeholder reduction.
- Added front, rear, left, and right accumulated body damage based on impact direction.
- Added independent engine damage stages at 100, 150, 200, and on-fire at 225.
- Changed handling degradation to follow engine state rather than linearly penalizing every point of body damage.
- Changed collision/wall lethality to clamp at one health, ignite, telegraph danger, and explode after a server-owned five-second fuse.
- Kept direct weapon lethality immediate when damage fully exhausts health.
- Added typed ignition events and source attribution across the fire fuse.
- Extended smoke, fire, condition HUD, and debug labels to use engine, fire, maximum-health, and localized damage state.

### QA

- Full suite: 19/19 passing, including the existing two-client multiplayer scenario.
- New assertions cover impact-zone classification, localized damage, engine thresholds, collision clamping, direct weapon destruction, fire-fuse timing, explosion/ejection, and complete restoration.
- Production TypeScript and Vite build: passing.
- `git diff --check`: clean.
- Live browser server: zero dropped simulation time and no new console errors.
- Live debug feed observed normal traffic collision losses of 21-26 health against the 1000-point model.

### Next Analysis

Resume the minimap only after reviewing mature radar implementations and GTA Online visibility semantics. The marker policy must account for local/remote players, police threat, occupied versus ambient vehicles, mission objectives, crews, interiors, and future area-of-interest filtering instead of drawing every entity unconditionally.

## 04:38 - Reference-Driven Online Minimap Checkpoint

### Research

- Inspected the production radar transform, dynamic range, edge normalization, tiled map streaming, player arrow, north marker, entity/coordinate blip types, display modes, alpha, and priority passes in `Radar.cpp/.h`.
- Reviewed official GTA Online Freemode, Executive, Special Cargo, Ghost Organization, Off the Radar, map filtering, and event visibility behavior.
- Recorded the behavior matrix and future server-privacy requirements in `docs/MINIMAP_RESEARCH.md`.

### Delivered

- Added a pure minimap marker policy independent of Phaser and canvas rendering.
- Added a canvas minimap renderer over the real exported district image.
- Added a north-up cropped local map with a directional local-player arrow.
- Added one remote marker per alive player, using their occupied vehicle's effective position and heading.
- Added police markers only while the local player has wanted heat.
- Intentionally excluded civilians and ambient traffic from normal radar clutter.
- Added objective, contact, shop, and property marker contracts for upcoming systems.
- Added deterministic marker priority, distance calculation, edge clamping, reduced edge alpha, and dynamic range from 520 on foot to 1100 at maximum driving speed.
- Added responsive desktop/touch placement and accessible marker/range state.

### QA

- Full suite: 22/22 passing.
- Added policy tests for local deduplication, occupied-player projection, ambient exclusion, wanted-police visibility, objective clamping, shop range, and bounded speed zoom.
- Added multiplayer state assertions for the researched 1000-health vehicle baseline and undamaged engine state.
- Production TypeScript and Vite build: passing.
- `git diff --check`: clean.
- Touch browser screenshot: map crop and four markers visible; no overlap with player health or movement control.
- Desktop geometry: 190x140 minimap at the lower left with no overlap against player HUD or debug control.
- Live canvas accessibility state: four markers at 520 range.

### Scaling Note

The current room replicates every player position. Future Off the Radar, Ghost Organization, interiors, stealth jobs, crews, and large districts require per-client server visibility; hiding a marker only in the renderer is not secure.

### Next Analysis

Research mission frameworks in the GTA source and mature multiplayer servers before implementing vehicle theft delivery. The system needs explicit scope/cleanup, objective phases, participant roles, reconnect behavior, payout idempotency, vehicle condition scoring, police consequences, failure reasons, debug state, and an event boundary suitable for the durable economy ledger.

## 04:52 - Freemode Group Mission Domain Checkpoint

### Research

- Inspected official GTA Online documentation for both instanced Heists and shared-session VIP/CEO work.
- Separated the two production models instead of forcing one mission abstraction to handle incompatible rules.
- Selected Freemode organization work for the first Boost and Deliver job: opt-in roster, shared-world objective, normal traffic and police, rival interference, and no private room.
- Reserved heist-specific property prerequisites, setup graphs, role slots, team lives, restarts, challenge continuity, and negotiated cuts for a later dedicated domain.
- Recorded source links, production behavior, and the NOCK0 contract in `docs/GROUP_MISSION_RESEARCH.md`.
- Added a free character creator and cosmetic-only progression policy to the feature research. Character appearance remains noncompetitive and fully unlocked during development.

### Delivered

- Added a Colyseus-independent Freemode mission domain with explicit forming, steal, lose-heat, deliver, completed, and failed phases.
- Added a short opt-in crew window, maximum four-player roster, explicit leader launch, and immutable post-launch payout roster.
- Enforced one active mission per participant and one reservation per target vehicle.
- Allowed any roster member to steal, drive, defend, or deliver the target.
- Made wanted heat a crew-wide delivery gate while keeping individual death nonfatal to the shared job.
- Added deterministic leader transfer to the earliest joined connected member.
- Added distinct target-destroyed, total-disconnect, timeout, and abandonment failures.
- Added target-condition reward scaling and one stable idempotency key per participant payout.
- Added bounded mission entity ownership with deterministic release/despawn cleanup records.
- Tracked connected/alive state, deaths, and active participation time for later contribution and anti-freeloading policy.

### QA

- Added eight mission tests for formation, capacity, locking, overlapping reservations, shared objective progress, team heat, death, leader transfer, failures, exactly-once payouts, reservation cleanup, and entity-scope cleanup.
- Full suite: 30/30 passing, including the existing two-client combat, passenger shooting, hijacking, driving, police, death, and respawn scenario.
- Production TypeScript and Vite build: passing.

### Next Analysis

Integrate the mission domain through a thin `DistrictRoom` adapter. Replicate only presentation state, publish typed lifecycle and payout events, reserve/promote one ambient traffic vehicle, expose contact/join/start controls, add world and minimap markers, and verify the complete crew flow with multiple browser clients before adding another mission template.

## 05:14 - Playable Freemode Mission and Room-Modularity Checkpoint

### Delivered

- Integrated Boost and Deliver into the shared district with a spawn-area contact, proximity-gated start/join commands, leader launch, 15-second automatic launch, a reserved ambient target, team heat, delivery, equal crew payout, and target return to traffic.
- Added replicated mission and participant presentation state without moving mission rules into schema classes.
- Added a compact responsive mission HUD, leader/join/abandon controls, server notices, objective timer, crew count, reward preview, world contact/target/delivery markers, and minimap objectives.
- Added mission phase, failure, and per-player payout events to the typed game event stream and `F3` history.
- Normalized fractional and non-finite traffic spawn seeds after live browser QA exposed a mission delivery-point crash.
- Moved all mission orchestration out of `DistrictRoom` into `FreemodeMissionController`; `MissionSystem` remains framework-independent.
- Moved crime registration, witness reporting, wanted heat, police dispatch, pursuit memory, and debug snapshots out of `DistrictRoom` into `CrimeResponseController` over the existing small policy modules.
- Added ADR 0004: the room is a network/schedule adapter, and future gameplay methods may not be added directly to it.

### Production Reference

- Rechecked the GTA III/Vice City ownership split across `Wanted.h/.cpp`, `EventList`, `Population`, `CopPed`, vehicle/ped modules, and running scripts.
- Preserved the transferable boundary: producers raise crimes, wanted owns crime response state, population owns ambient actors, cops own individual tactics, and scripts own mission progression.
- Kept NOCK0 server-specific concerns such as authoritative multiplayer, idempotent payouts, schema projection, and client notices in explicit adapters instead of copying the C++ globals.

### QA

- Full suite: 32/32 passing.
- Production TypeScript and Vite build: passing.
- Two real Colyseus clients form a crew, join the same replicated mission, lock the roster, reserve the same target, and launch synchronized work.
- Deterministic room-adapter scenario completes steal, lose-heat, deliver, equal payout, payout deduplication, terminal retention, occupant release, and return-to-traffic cleanup.
- Desktop and mobile browser captures show a nonblank map and readable HUD with no incoherent overlap.
- Live `F3` history shows mission phase transitions; minimap marker count increases when the objective becomes active.

### Next Analysis

Continue shrinking `DistrictRoom`. Extract vehicle lifecycle/seating/driving and traffic routing behind separate owners, then extract combat/projectiles and pedestrian behavior. Preserve one explicit simulation coordinator and keep every extraction behavior-compatible with the multiplayer scenario.

## 05:20 - Vehicle Access Extraction Checkpoint

### Delivered

- Added `VehicleAccessController` as the sole owner of nearby-car selection, enter and hijack timing, four-seat assignment, exits, driver promotion, action cleanup, and player removal from vehicles.
- Kept carjacking consequences modular through narrow callbacks: traffic control release, ejected-driver creation, and crime recording.
- Changed room message handling, player action updates, disconnect/death cleanup, vehicle destruction, passenger promotion, projectile occupant checks, and mission delivery cleanup to use the controller API.
- Added an explicit test fixture for controller construction instead of allowing direct room tests to depend on hidden fallback behavior.
- Reduced `DistrictRoom` from 1,549 to 1,411 lines in this checkpoint and from 1,704 to 1,411 across the mission/crime/access extraction sequence.

### QA

- Full suite: 32/32 passing, including hijacking, ejected witnesses, four-seat multiplayer, passenger shooting, driver promotion behavior, vehicle destruction/ejection, mission cleanup, and projectile occupant exclusion.
- Production TypeScript and Vite build: passing.
- `git diff --check`: clean.

### Next Analysis

Split ambient traffic route state from authoritative vehicle physics. Traffic should provide AI controls/routes through a public API; vehicle simulation should own handling, impacts, localized damage, fire, destruction, restoration, and occupant synchronization without absorbing seating or crime policy.

## 05:24 - Ambient Traffic Ownership Checkpoint

### Delivered

- Added `TrafficController` as the owner of per-car route cells, cruise speed, deterministic forward/turn decisions, lane following, obstacle recovery, hijack braking, registration, and release.
- Removed traffic runtime maps and route-planning methods from `DistrictRoom`.
- Changed district population spawn, hijacking, vehicle restoration, mission delivery cleanup, and live traffic updates to use the traffic API.
- Kept traffic routing separate from vehicle collision/damage and from vehicle access/seating.
- Reduced `DistrictRoom` from 1,411 to 1,334 lines.

### QA

- Added a deterministic traffic scenario proving identical seeded routes, real road movement, hijack braking, and no movement after release.
- Full existing suite: 32/32 passing before the focused traffic scenario.
- Focused traffic scenario: passing.
- Production TypeScript and Vite build: passing.
- Existing multiplayer test still observes all eight ambient traffic cars moving.

### Next Analysis

Extract authoritative vehicle simulation next: player controls, occupant synchronization, world/vehicle/pedestrian impacts, localized mechanical damage, ignition, explosion, and restoration. Keep traffic as an input owner and vehicle access as the seating owner.

## 05:31 - Authoritative Vehicle Simulation Extraction

### Delivered

- Added `VehicleSimulationController` as the owner of player-driven handling, wall impacts, traffic/pedestrian impacts, collision-pair deduplication, collision impulse application, and occupant position projection.
- Moved the researched 1000-point mechanical model, localized body damage, engine degradation, ignition fuse, destruction, ejection, crash injury, restoration, and typed vehicle events out of `DistrictRoom`.
- Moved mission return-to-traffic restoration into the vehicle simulation API while keeping route registration in `TrafficController` and seats in `VehicleAccessController`.
- Changed projectiles to call the vehicle simulation damage API rather than room-owned mechanics.
- Removed vehicle impact cooldown, collision-pair, and fire-source runtime maps from the room.
- Changed `TrafficController.update` to report road advancement; vehicle simulation now owns the resulting pedestrian-impact check.
- Reduced `DistrictRoom` from 1,334 to 1,004 lines and from 1,704 to 1,004 across the modular extraction sequence.

### QA

- Full suite: 33/33 passing.
- Production TypeScript and Vite build: passing.
- Existing tests still cover deterministic traffic, collision impulses, separating contacts, localized mechanical damage, ignition delay, explosion, occupant ejection, restoration, weapon damage, mission cleanup, and two-client driving/passenger combat.
- Direct room fixtures now construct explicit access, traffic, and simulation dependencies through shared test support.
- `git diff --check`: clean.

### Next Analysis

Extract combat as separate fire-control, projectile, damage, and player-lifecycle boundaries. Preserve authoritative ammunition/cooldowns, passenger shooting, vehicle interception, crime events, kill rewards, and deterministic deferred projectile cleanup.

## 05:41 - Production-Reference Combat Extraction

### Research

- Reviewed `CWeaponInfo` as immutable weapon content, `CWeapon` as holder fire/reload/ammo state, `CBulletInfo` as a bounded projectile owner, separate projectile/explosion/fire managers, and victim-owned ped/vehicle damage response.
- Recorded the clean-room behavior map and authoritative multiplayer adaptations in `docs/COMBAT_SYSTEM_RESEARCH.md` before changing the room.

### Delivered

- Added `FireControlController` for holder validation, driver/passenger rules, per-player cooldown, ammunition, deterministic spread, shotgun pellets, seat-correct origins, weapon cycling, and bullet creation.
- Added `ProjectileController` for lifetime, movement, swept collision, police wanted gating, occupied-source-vehicle exclusion, target-family routing, vehicle impact zones, and deferred removal.
- Added `DamageController` for player/NPC health, typed damage/death events, crime severity, civilian panic, NPC respawn scheduling, and current street-cash rewards.
- Added `PlayerLifecycleController` for death, vehicle removal, wanted cleanup, input reset, respawn delay/location, health, ammunition refill, and respawn events.
- Changed police fire, player fire messages, weapon cycling, vehicle impact callbacks, projectiles, and the fixed simulation schedule to use those owners.
- Removed weapon/projectile IDs, player shot cooldowns, damage, kill, and respawn methods from `DistrictRoom`.
- Reduced `DistrictRoom` from 1,004 to 755 lines and from 1,704 to 755 across all modular extraction checkpoints.

### QA

- Full suite: 34/34 passing.
- Added direct fire-control coverage for cooldown, ammunition, shotgun pellet count, driver denial, and passenger world origin.
- Existing swept projectile, occupied vehicle exclusion, vehicle damage, crime, kill reward, passenger shooting, death, wanted reset, and respawn scenarios remain green.
- Real two-client Colyseus scenario remains passing.
- Production TypeScript and Vite build: passing.
- `git diff --check`: clean.

### Next Analysis

Extract pedestrian population/runtime and behavior next. Preserve lower-frequency deterministic thinking, civilian panic/threat memory, police pursuit/search consumption, police fire requests, collision-safe locomotion, death/respawn, and ejected-driver creation without letting the pedestrian controller absorb crime or projectile rules.

## 05:53 - Production-Reference Pedestrian Extraction

### Research

- Reviewed the GTA III/Vice City ownership split across `CPopulation`, `CCivilianPed`, `CCopPed`, common `CPed` state/objectives, pedestrian routes, and placement validation.
- Recorded the clean-room production findings, NOCK0 boundary, future internal layers, and acceptance tests in `docs/PEDESTRIAN_SYSTEM_RESEARCH.md` before implementation.
- Preserved the transferable split: population owns ambient actor lifecycle, pedestrians own individual state and locomotion, wanted/dispatch owns response assignment, and combat owns firing/projectiles/damage.

### Delivered

- Added `PedestrianController` as the owner of synchronized NPC creation and private wander, think, fire, panic, threat, and respawn runtime.
- Moved deterministic ambient movement, collision-safe locomotion, civilian threat response, police pursuit/search consumption, police fire requests, death recovery, and ejected-driver creation out of `DistrictRoom`.
- Kept wanted/dispatch and projectile ownership outside pedestrians through narrow police-target and fire-request ports.
- Cleared stale panic/threat memory on NPC respawn so a new life does not inherit a prior threat.
- Added a reusable pedestrian test fixture and removed the final test dependency on the room's former private ejected-driver method.
- Documented that the facade must split internally into population policy, perception, behavior, navigation, and locomotion as AI depth grows.
- Reduced `DistrictRoom` from 755 to 608 lines and from 1,704 to 608 across the modular extraction sequence.

### QA

- Added five direct pedestrian scenarios covering deterministic 13-actor population, collision-safe placement, panic direction, assigned police pursuit and fire cooldown, delayed archetype respawn, and carjacking ejection.
- The first complete run exposed a stale vehicle test fixture; replacing its private room call with an explicit pedestrian dependency fixed it.
- A subsequent complete run had one distance-sensitive multiplayer revenge-shot timeout; the isolated integration rerun passed, then the final full suite passed 39/39.
- Real two-client Colyseus scenario remains passing across weapons, seats, driving, passenger fire, PvP death, wanted response, and clean respawn.
- Production TypeScript and Vite build: passing.
- Live browser QA: online at 1280x720 with 1 player, 13 NPCs, 11 vehicles, 25 spatial records, zero dropped time, zero deferred backlog, working debug entity labels, and authoritative pistol-to-SMG cycling.
- `git diff --check`: clean.

### Next Analysis

Extract player input, locomotion, command validation, and district population assembly so the room becomes a pure transport/schedule adapter. Then separate debug/state projection and begin the same modularity pass on the client scene before adding deeper pedestrian states or another feature family.

## 06:01 - Production-Reference Player Control Extraction

### Research

- Reviewed the GTA III/Vice City control pipeline across `CPad`, `CPlayerPed::ProcessControl`, mode-specific on-foot control, player weapon processing, and `CPlayerInfo` interaction/lifecycle ownership.
- Recorded the clean-room ownership map, authoritative multiplayer adaptation, future command sequencing/reconciliation requirements, and acceptance tests in `docs/PLAYER_CONTROL_RESEARCH.md` before implementation.
- Preserved the transferable split between raw intent, state-gated locomotion, vehicle control, weapon control, vehicle access, and player lifecycle.

### Delivered

- Added `PlayerControlController` as the sole owner of connected-player move intent, finite/clamped wire input, aim validation, driver input exposure, on-foot movement, collision resolution, reset, and unregister cleanup.
- Moved all player runtime input state, move/aim message interpretation, on-foot movement math, and angle/input helpers out of `DistrictRoom`.
- Routed death input reset and vehicle simulation input through the public player-control API.
- Preserved full keyboard speed while fixing partial analog/touch input so magnitude is retained; diagonal input is capped to unit speed.
- Replaced test fixture access to the removed room runtime map with an explicit player-control fixture.
- Hardened the two-client PvP/respawn test to approach effective range and re-aim from current replicated positions before each shot instead of relying on one stale angle through a dynamic world.
- Reduced `DistrictRoom` from 608 to 562 lines and from 1,704 to 562 across the modular extraction sequence.

### QA

- Added four direct controller scenarios covering hostile/non-finite input, clamping/reset/unregister, analog and diagonal speed, axis collision, death/action/vehicle gating, and on-foot/passenger/driver aim rules.
- The previously intermittent late PvP timeout was reproduced, corrected at the scenario level, and passed repeated isolated runs.
- Full suite: 43/43 passing, including real two-client movement, driving, passenger aim/fire, PvP death, wanted cleanup, and respawn.
- Production TypeScript and Vite build: passing.
- `git diff --check`: clean.

### Next Analysis

Extract deterministic district population assembly so map content, ambient actor counts, parked vehicles, traffic archetypes, and spawn policy become a tested world bootstrap owner rather than a room method.

## 06:11 - District Population Bootstrap Extraction

### Research

- Reviewed the GTA III/Vice City population pipeline across `CPopulation`, `CCarCtrl`, `CTheZones`, and `CPathFind` for actor budgets, model classes, density, spawn validation, route/lane ownership, and protected entity lifecycles.
- Recorded the clean-room bootstrap boundary and the production requirements for later dynamic zone, interest-area, visibility, streaming, spawn/despawn, and entity-ownership policy in `docs/DISTRICT_POPULATION_RESEARCH.md` before implementation.

### Delivered

- Added `DistrictPopulationController` as the owner of mission-contact placement and initial district population composition.
- Moved ten civilian, three police, three parked/starter vehicle, and eight ambient traffic spawns out of `DistrictRoom`.
- Routed pedestrians through `PedestrianController`, ambient routes through `TrafficController`, and vehicle health through authoritative vehicle config.
- Made bootstrap idempotent so repeated setup cannot duplicate actors, traffic runtime, or spatial callbacks.
- Exported the common authoritative vehicle footprint from vehicle config for bootstrap and spatial projection instead of restoring a room-local constant.
- Reduced `DistrictRoom` from 562 to 506 lines and from 1,704 to 506 across the modular extraction sequence.

### QA

- Added deterministic bootstrap coverage for exact archetype counts, IDs, models, positions, headings, health, collision-safe footprints, road-only traffic, mission contact, idempotency, callback counts, route registration, and real traffic movement.
- Full suite: 45/45 passing, including the real two-client scenario.
- Production TypeScript and Vite build: passing.
- Live browser debug: 1 player, 13 NPCs, 11 vehicles, 25 spatial entities, zero dropped time, and zero deferred backlog.
- `git diff --check`: clean.

### Next Analysis

Extract debug snapshot/event projection from `DistrictRoom`, then audit remaining room methods against ADR 0004. After the server adapter is lean, start the equivalent presentation/input/rendering split in `DistrictScene` without touching concurrent map-pipeline work.

## 06:16 - Debug Projection and Server-Room Audit

### Research

- Reviewed the GTA III/Vice City separation across bounded `CDebug` lines, `CEventList` world facts, and `CReplay` capture/playback buffers.
- Recorded NOCK0's distinction between developer snapshots, structured logs, metrics, traces, audits, anti-cheat evidence, analytics, and deterministic replay in `docs/DEBUG_OBSERVABILITY_RESEARCH.md` before implementation.
- Added explicit requirements for cadence/bounds, role-gated access, redaction, high-cardinality data, backpressure, state hashes, and on-chain/economy correlation.

### Delivered

- Added `DebugSnapshotController` as the owner of typed-event summaries, bounded history, six-tick sampling, state/queue/clock counters, incident/pursuit protocol copies, and debug publication.
- Moved debug enablement, history, cadence, protocol shaping, event formatting, and transport payload construction out of `DistrictRoom`.
- Changed the room to drain typed frame events once and pass them directly to the read-only projector.
- Audited the remaining room surface: dependency construction, Colyseus command registration, join/leave transport lifecycle, mission notices, explicit fixed-step schedule, and spatial projection.
- Reduced `DistrictRoom` from 506 to 425 lines and from 1,704 to 425 across the modular extraction sequence.

### QA

- Added disabled-mode, bounded-history, six-tick cadence, state/queue count, incident/pursuit copy, event-count, and summary-format coverage.
- Full suite: 48/48 passing, including the real two-client scenario.
- Production TypeScript and Vite build: passing.
- Live browser `DBG` remains online with correct 1/13/11/25 player/NPC/vehicle/spatial counts and zero dropped/deferred pressure.
- `git diff --check`: clean.

### Next Analysis

Begin the equivalent client modularity pass by inventorying `DistrictScene` responsibilities and extracting presentation helpers around the concurrent original-map pipeline. Keep Phaser scene lifecycle and input/network adaptation in the scene while moving entity rendering, HUD/minimap projection, debug drawing, and effects into focused owners.

## 06:24 - Browser Input and Command Extraction

### Research

- Rechecked `CPad`, `CPlayerPed`, and `CPlayerInfo` boundaries for device sampling, current/previous control state, disable masks, edge controls, locomotion/weapon gating, and enter/exit intent.
- Recorded the browser-specific policy/adapter split, listener lifecycle, neutral-input flush, cadence, focus/accessibility requirements, and future sequenced-command/reconciliation work in `docs/CLIENT_INPUT_RESEARCH.md` before implementation.
- Inventoried `DistrictScene` at 1,373 lines across world setup, networking, entity rendering, input, prediction, interpolation, mission UI, debug UI, HUD, minimap, and effects.

### Delivered

- Added pure `client-input-policy.ts` for combined keyboard/touch normalization, analog magnitude, replicated-state weapon gates, movement change/heartbeat, and independent aim/fire/weapon cadence.
- Added `ClientInputController` for Phaser keyboard/pointer/wheel, DOM weapon/vehicle/mission controls, touch input, command publication, and local aim callbacks.
- Changed `DistrictScene` to consume one movement vector and one aim callback instead of owning device fields, command timers, mission action dispatch, and weapon/vehicle DOM listeners.
- Added scene-shutdown teardown with neutral movement plus wheel/DOM/media-query/button/pointer listener removal.
- Added `src/game/README.md` with client dependency rules and next extraction targets.
- Reduced `DistrictScene` from 1,373 to 1,249 lines without touching the concurrent original-map content pipeline.

### QA

- Added pure coverage for analog preservation, combined-input unit cap, non-finite normalization, movement change/heartbeat, independent aim/fire/weapon cadence, and dead/action/driver/passenger gates.
- Full suite: 51/51 passing, including the real two-client scenario.
- Production TypeScript and Vite build: passing.
- Live browser: weapon button changed pistol/120 to SMG/240; Enter Car opened vehicle HUD and EXIT CAR; Exit Car restored the on-foot weapon HUD.
- `git diff --check`: clean.

### Next Analysis

Extract replicated entity creation/synchronization/interpolation behind a renderer facade, beginning with bullets and NPCs before players/vehicles. Preserve passenger peek/fire animation, labels, depth ordering, vehicle damage effects, and local prediction correction.

## 06:34 - NPC and Projectile Renderer Extraction

### Research

- Reviewed production ownership across GTA III/Vice City `CPools`, `CRenderer`, `CPed::PreRender`/`Render`, and the bounded `CBulletInfo` manager before changing the client.
- Recorded the clean-room entity-cache lifecycle, explicit removal, render-pass ownership, pooling thresholds, interpolation requirements, and acceptance tests in `docs/CLIENT_RENDERING_RESEARCH.md`.

### Delivered

- Added `PedestrianRenderer` as the owner of NPC render-object creation, replicated targets, visibility, walking animation, rotation, interpolation, depth, removal, and shutdown.
- Added `ProjectileRenderer` as the owner of bullet render-object creation, weapon/police visual policy, target synchronization, interpolation, muzzle flashes, removal, and shutdown.
- Added pure interpolation and projectile-style policies so correction thresholds, wrapped angles, and visual selection are testable without Phaser.
- Preserved passenger peek recoil through a narrow projectile-created callback instead of coupling projectile presentation to the player cache.
- Reduced `DistrictScene` from 1,249 to 1,161 lines and removed direct NPC/projectile cache iteration from the scene coordinator.
- Hardened the two-client integration helper with bounded obstacle detours so the revenge-fire scenario verifies combat behavior rather than depending on a collision-free straight line.

### QA

- Added focused coverage for ordinary correction, large-error snapping, interpolation clamping, shortest wrapped rotation, weapon projectile styles, and the police projectile override.
- Full suite: 54/54 passing, including real two-client movement, shared vehicles, driving, passenger fire, PvP death, wanted reset, and respawn.
- Production TypeScript and Vite build: passing.
- Live browser: district rendering remained stable after synchronization; firing visibly created the projectile/muzzle effect and reduced pistol ammo from 120 to 119.
- `git diff --check`: clean before checkpoint.

### Next Analysis

Make debug snapshot delivery an explicit client subscription lifecycle so the server never emits snapshots before a handler exists. Then extract player and vehicle renderers while preserving prediction, labels, seats, passenger peek animation, damage smoke/fire, and local vehicle ownership presentation.

## 06:38 - Explicit Debug Snapshot Subscription

### Analysis

- Traced the recurring Colyseus warning to room-wide debug broadcasts beginning before Phaser registered its `debug.snapshot` handler.
- Kept projection and transport separate: `DebugSnapshotController` still produces bounded plain snapshots, while the room only routes them to explicitly subscribed sessions.

### Delivered

- Added shared `debug.subscribe` and `debug.unsubscribe` protocol commands.
- Added `DebugSnapshotSubscription`, which installs the message handler before opting in, prevents duplicate starts/stops, unsubscribes on scene shutdown, and removes the Colyseus listener.
- Added room-scoped subscriber tracking, initialization cleanup, leave cleanup, and targeted snapshot delivery instead of room-wide broadcast.
- Updated the real multiplayer client setup to subscribe only after handlers are registered.

### QA

- Added direct lifecycle coverage for handler-first ordering, idempotent start/destroy, protocol messages, snapshot delivery, and listener removal.
- The two-client integration scenario proves snapshots are absent before opt-in and available after opt-in while the rest of the game flow remains intact.
- Full suite: 55/55 passing. Production TypeScript and Vite build: passing.
- Fresh live-browser reload produced no new unhandled `debug.snapshot` warning.
- Live DBG panel populated at tick 1,449 with 3 players, 13 NPCs, 11 vehicles, 27 spatial entities, zero dropped time, and zero deferred backlog.

### Next Analysis

Extract player and vehicle renderers with pure presentation policies first. Preserve local prediction correction, player nameplates, seat composition, passenger peek/fire animation, traffic/local vehicle distinction, damage smoke/fire, destruction, and depth ordering.

## 06:49 - Player and Vehicle Renderer Extraction

### Research

- Reviewed `CPed::Render`, vehicle occupant slots/transition flags, enter/hijack animation callbacks, and `CAutomobile::PreRender`, `Render`, and `AddDamagedVehicleParticles` before implementation.
- Recorded the clean-room player/vehicle ownership boundary, staged damage model, seat composition, camera/HUD callbacks, future LOD/pooling requirements, and acceptance tests in `docs/PLAYER_VEHICLE_RENDERING_RESEARCH.md`.

### Delivered

- Added `PlayerRenderer` as the owner of player, passenger, weapon, and nameplate render-object lifecycle; local prediction; remote interpolation; action presentation; seat composition; passenger peek/recoil; and deterministic teardown.
- Added `VehicleRenderer` as the owner of vehicle containers, body models, police lights, interpolation, depth, staged damage smoke/fire/tint, read-only pose lookup, and deterministic teardown.
- Added pure player/vehicle presentation policies for weapon model sizing, three passenger seat anchors, recoil offsets, vehicle frames, and healthy/damaged/smoking/burning/wrecked stages.
- Replaced cross-renderer object access with a read-only vehicle pose contract and narrow local-state/local-occupancy callbacks for HUD and camera follow.
- Improved passenger aiming origin to use the effective seat anchor instead of a hidden on-foot sprite.
- Reduced `DistrictScene` from 1,161 to 834 lines, and from 1,373 to 834 across the client modularity pass.
- Hardened multiplayer seat assertions to wait for the complete vehicle/seat patch on both clients, and made the revenge-fire scenario use a verified close engagement range with actionable failure diagnostics.

### QA

- Added coverage for every weapon model, all passenger anchors, recoil direction/scale, every vehicle frame, and all five current vehicle visual stages.
- Full suite: 57/57 passing. Production TypeScript and Vite build: passing.
- The real two-client integration scenario passed twice consecutively after race/geometry hardening.
- Live browser: player/vehicle sprites remained stable; names followed cars; weapon cycling selected SMG; enter switched to the vehicle camera/HUD; exit restored SMG/240; no new runtime errors appeared.

### Next Analysis

Extract camera follow/damage feedback, HUD/toast projection, mission world/HUD presentation, and debug rendering as separate owners. Then return to gameplay depth with richer pedestrian perception/behavior/navigation states backed by scenario tests and the production reference.

## 06:51 - Added OpenGTA2 3D Migration Track

### Added To Roadmap

- Added `docs/OPENGTA2_3D_MIGRATION_REPORT.md` as the detailed plan for restoring GTA2's real block height, walls, slopes, partial geometry, shading, and depth-buffer occlusion in the browser.
- Added an eight-step parallel track to the ranked gameplay roadmap: renderer-neutral offline geometry, one validated chunk, feature-flagged Three viewer, district export, network actor parity, raycast/input/HUD/mobile parity, controlled default switch, then authoritative elevation.
- Updated the engineering roadmap so Phaser remains the working fallback until the Three renderer has measured parity; the current street loop and server modularity are not blocked on a renderer rewrite.
- Preserved the licensing boundary: Rockstar-derived world content remains a private compatibility fixture and public release requires original geometry/layout and art.

### Next Analysis

Keep the current client extraction sequence moving, then scope the first renderer-neutral `8 x 8` geometry export as an isolated tool milestone with visual fixture tests. Do not add server `z` or multi-level collision before the browser geometry proof is correct.

## 06:54 - Camera Presentation Extraction

### Research

- Reviewed GTA III/Vice City `CCamera` target ownership, follow-ped/follow-car modes, jump-cut restoration, control priority, and transient decaying shake before implementation.
- Recorded the clean-room camera boundary, renderer-adapter contract, lifecycle requirements, and future mission/spectator/death/debug priorities in `docs/CAMERA_PRESENTATION_RESEARCH.md`.

### Delivered

- Added pure camera policy for the mobile/desktop zoom breakpoint, distinct player/vehicle follow lerp, initial centering, and stable target keys.
- Added `CameraPresentationController` as the owner of Phaser bounds/background, target identity, follow transitions, local damage shake/flash, responsive resize handling, and shutdown teardown.
- Removed direct camera calls and the anonymous resize listener from player/vehicle callbacks in `DistrictScene`.
- Reduced `DistrictScene` from 834 to 822 lines and made camera decisions portable to the future Three adapter.

### QA

- Added policy coverage for both sides of the responsive breakpoint, player/vehicle smoothing differences, centering mode, and target identity.
- Full suite: 58/58 passing. Production TypeScript and Vite build: passing; focused camera/render policy suite: 6/6 passing.
- Live browser completed player-to-vehicle-to-player transitions with the correct vehicle/on-foot HUD and no new runtime errors.

### Next Analysis

Extract local HUD/toasts/connection state behind a pure projection and DOM adapter, including deterministic transition notices and listener/timer teardown. Keep mission and debug presentation separate.

## 07:01 - Local HUD and Notice Extraction

### Research

- Reviewed GTA III/Vice City `CHud` ownership of meters, weapon models, visibility, current/previous messages, channel-specific state, timers, fades, and repeated-frame suppression before implementation.
- Recorded the clean-room fact-to-projection boundary, initial-sync behavior, mode stability, bounded messaging, lifecycle, and future channel priorities in `docs/HUD_PRESENTATION_RESEARCH.md`.

### Delivered

- Added pure HUD projection for name, cash, health, wanted heat, death, driver/passenger/foot mode, weapon model/ammo, speed, and vehicle condition.
- Added pure previous/current transition policy for one-shot wanted escalation/loss, cash gain, entering, and hijacking notices; initial synchronization no longer falsely announces existing state.
- Added `LocalHudController` with cached DOM references, a six-message bounded/deduplicated notice queue, mission-notice input, connection state, and deterministic timer/queue teardown.
- Removed all local HUD DOM queries, transition memory, toast timeout, and connection display logic from `DistrictScene`.
- Reduced `DistrictScene` from 822 to 734 lines and from 1,373 to 734 across the client modularity pass.

### QA

- Added foot/driver/passenger/dead projection coverage plus health/condition clamping, weapon/ammo, speed, initial-sync suppression, transition ordering, and repeat suppression.
- Full suite: 60/60 passing. Production TypeScript and Vite build: passing.
- Live browser weapon cycling updated pistol to SMG/240 through the new DOM adapter; connection remained online and no new runtime errors appeared.
- The two-client integration suite continues to cover shared driver/passenger state, passenger weapons, exit, death, wanted cleanup, and respawn.

### Next Analysis

Extract Freemode mission HUD/world markers behind a mission presentation policy/controller, then extract the debug panel/world overlay. Preserve mission phase visibility, participant actions, objective clamping, cleanup, and F3/DBG behavior.

## 07:06 - Freemode Mission Presentation Extraction

### Research

- Reviewed production separation between mission scripts, `CUserDisplay::OnscnTimer`, HUD timer/counter/title channels, radar blips, 3D markers, and script-owned cleanup before implementation.
- Recorded the clean-room state-to-HUD/world/minimap boundary, action permissions, lifecycle, renderer-adapter requirements, and acceptance tests in `docs/MISSION_PRESENTATION_RESEARCH.md`.

### Delivered

- Added pure mission presentation policy for deterministic active/joinable selection, distance/capacity checks, phase objectives, timer/payout metadata, participant/leader controls, minimap points, target markers, and delivery zones.
- Added `MissionPresentationController` as the owner of mission DOM, action listener and command dispatch, replicated-state reference, Phaser contact/target/delivery graphics, and shutdown cleanup.
- Removed all mission selection, formatting, DOM mutation, world drawing, minimap projection, and action dispatch from `DistrictScene` and generic `ClientInputController`.
- Unified world and minimap objectives behind the same authoritative phase projection.
- Reduced `DistrictScene` from 734 to 541 lines and from 1,373 to 541 across the client modularity pass.

### QA

- Added contact/start, nearby join, distance/capacity rejection, leader launch, active abandon, terminal phase, timer, payout, target, delivery, world, and minimap projection coverage.
- Full suite: 63/63 passing. Production TypeScript and Vite build: passing.
- Live browser: START JOB projected the forming panel and LAUNCH control; LAUNCH projected the 3:00 steal objective, ABANDON control, sixth minimap marker, and mission notice with no new runtime errors.

### Next Analysis

Extract the F3/DBG panel, collision/spatial/entity world overlay, and debug label cache behind a debug presentation controller. Then audit `DistrictScene` down to world bootstrap, controller wiring, update order, collision query, minimap/crosshair coordination, and state fan-out.

## 07:13 - Debug Presentation Extraction and Scene Audit

### Research

- Reused the production separation already documented across bounded `CDebug` lines, `CEventList` world facts, and `CReplay` capture, then audited every remaining client debug responsibility against it.
- Preserved the existing separation between server projection, opt-in transport, pure client projection, DOM panel, and Phaser world overlay.

### Delivered

- Added pure debug panel projection with replicated-state fallbacks, authoritative snapshot counters, clock/drop formatting, incident/pursuit counts, and bounded event text.
- Added `DebugPresentationController` as the owner of F3/button input, opt-in subscription composition, cached panel DOM, 100 ms draw cadence, collision tiles, spatial grid, player/NPC/vehicle/bullet geometry, incidents, pursuits, label cache, and teardown.
- Removed all debug keys, toggle listeners, snapshot storage, DOM formatting, graphics, labels, cadence, and world-drawing code from `DistrictScene`.
- Audited the remaining scene surface: asset/world bootstrap, focused-controller wiring, replicated-state fan-out, explicit frame order, local collision query, vehicle-action affordance, minimap coordination, and crosshair.
- Reduced `DistrictScene` from 541 to 266 lines and from 1,373 to 266 across the client modularity pass. `DistrictRoom` remains a 442-line authoritative transport/schedule adapter after its own 1,704-line extraction.

### QA

- Added fallback/snapshot panel coverage for counts, clock, dropped/deferred pressure, incidents, pursuits, and event summaries.
- Full suite: 65/65 passing. Production TypeScript and Vite build: passing.
- Live DBG: 4 players, 13 NPCs, 11 vehicles, 28 spatial records, zero dropped/deferred pressure, eight recent events, visible collision/spatial/entity overlays, and no new runtime errors.

### Next Analysis

Return to gameplay depth. Split the current pedestrian facade into perception, behavior, navigation, and locomotion only as richer production-referenced states are added; begin with event perception/memory and civilian threat responses, retaining deterministic scenario tests and fixed simulation budgets.

## 07:19 - Layered Pedestrian AI Extraction

### Research

- Revisited GTA III/Vice City pedestrian population, `CPed` state/objective ownership, civilian threat response, police tactics, route/placement helpers, and separate decision/locomotion timing before changing behavior.
- Kept the room-facing facade while assigning perception, decisions, navigation recovery, locomotion, and private runtime memory to separate modules inside the pedestrian domain.

### Delivered

- Added explicit private `wander`, `flee`, `pursue`, and `search` objectives plus last-known threat position, think/fire deadlines, temporary navigation detours, and respawn state.
- Added `PedestrianPerceptionSystem` for pursuit observations and expiring civilian threat memory; civilians continue fleeing the last-known location during the panic window if a player temporarily disappears.
- Added `PedestrianBehaviorSystem` for ambient, civilian, and police intent. Police target aim is now independent of temporary collision-detour movement.
- Added deterministic `PedestrianNavigationSystem` recovery and continuous per-axis `PedestrianLocomotionSystem` collision movement.
- Reduced `PedestrianController` from 245 to 176 lines; it now owns creation/ejection/respawn lifecycle and composes the layers rather than accumulating AI details.

### QA

- Added focused tests for last-known threat expiry, police detour versus aim/fire cadence, deterministic blocked-path recovery, and independent axis collision resolution.
- Existing pedestrian population, panic, police pursuit/fire, death/respawn, and carjacking-driver scenarios remain green.
- Full suite: 68/68 passing. Production TypeScript and Vite build: passing.
- `git diff --check`: clean before checkpoint.

### Next Analysis

Model production-style bounded world stimuli for gunshots, impacts, injuries, deaths, and later explosions. Feed them into pedestrian perception through typed event facts with independent expiry and update budgets, then add bravery/archetype response choices without coupling AI to combat internals.

## 07:28 - Event-Driven Pedestrian Perception

### Research

- Reviewed the production `CEventList` bounded slots, semantic refresh, expiry pass, nearest-event query, and separation between perceptible facts and crime reporting.
- Defined the clean-room boundary in `docs/PEDESTRIAN_EVENT_PERCEPTION_RESEARCH.md`: ordered game events, durable crime incidents, and short-lived pedestrian stimuli remain distinct owners.

### Delivered

- Added one authoritative `weapon.fired` event per trigger action, including one event for a multi-pellet shotgun blast and police fire.
- Added a 128-entry `PedestrianStimulusRegistry` with input validation, semantic deduplication/refresh, deterministic capacity eviction, expiry, and severity-distance selection.
- Added `PedestrianStimulusAdapter` for gunshots, non-weapon impacts, injuries, deaths, fires, and explosions; weapon hits intentionally do not also create impact facts.
- Added staggered 240 ms stimulus perception and private expiring memory while retaining fixed-step locomotion.
- Added deterministic bravery traits: civilians flee high-severity danger but may investigate low-severity impacts, while unassigned police investigate and explicit police threats approach rather than flee.
- Preserved dispatch pursuit as the higher-priority police observation and kept projectile/damage/wanted ownership outside pedestrian AI.

### QA

- Added registry capacity/refresh/scoring/expiry, event-adapter, perception cadence, memory, civilian response, and police response scenarios.
- Extended fire-control coverage to prove cooldown rejection emits no event and shotgun pellets emit one event.
- Full suite: 71/71 passing, including the real two-client combat/vehicle/death/respawn scenario.
- Production TypeScript and Vite build: passing.

### Next Analysis

Project objective, bravery, personal-threat memory, stimulus memory, and active stimulus radii through the opt-in F3 diagnostics. Then perform live browser QA by firing near civilians and verifying flee/investigate transitions without adding persistent network state to ordinary clients.

## 07:35 - Inspectable Pedestrian AI Diagnostics

### Delivered

- Extended the opt-in debug protocol with copied pedestrian AI and active-stimulus diagnostics; ordinary clients still receive no persistent AI runtime state.
- Added deterministic controller diagnostics for objective, bravery, personal threat, panic deadline, stimulus kind/source, and stimulus deadline.
- Added an F3 `Stimuli` pressure counter plus world circles for stimulus center/radius, severity, and remaining lifetime.
- Extended NPC debug labels with current objective, bravery, and active threat or stimulus memory.
- Added a stimulus legend entry and preserved lifecycle cleanup through the existing debug presentation owner.

### QA

- Added server projection copy-isolation assertions and controller diagnostic coverage.
- Production TypeScript and Vite build: passing; focused debug/pedestrian suites: passing.
- Live browser F3 projected 13 NPC diagnostics, active stimulus counts, a full event radius with severity/TTL, and a police `investigate` response.
- A clean browser reload remained online with no new runtime errors. Historical console entries were traced to hot module replacement while earlier scene fields were being extracted, not the current bundle.
- The first post-diagnostics full run exposed a geometry-dependent revenge-fire test: range was valid but a wall could still clip the lane. The helper now requires authoritative map line of sight as well as proximity.
- The real two-client scenario passed twice consecutively after the correction; the final full suite passed 71/71.

### Next Analysis

Use the new diagnostics to deepen visible civilian behavior with a production-referenced reaction state machine: orient/look, startled pause, flee route, safe-distance recovery, and police-call intent. Define replicated presentation intent separately so animation can become richer without deciding authoritative outcomes.

## 07:47 - Staged Civilian Reactions and Presentation Intent

### Research

- Reviewed production civilian threat scanning, short look timers, flee origins/deadlines, investigation approach/stop behavior, temperament branching, repeated-event lifetime extension, and explicit previous-state restoration.
- Recorded the original clean-room state and presentation contract in `docs/PEDESTRIAN_REACTION_RESEARCH.md`.

### Delivered

- Added a dedicated `PedestrianReactionSystem` with explicit `orient`, `respond`, and `recover` phases instead of expanding the behavior facade.
- High-severity cues now produce a brief bravery-scaled startle toward danger, committed flight, safe-distance/deadline recovery, then ambient restoration.
- Braver civilians investigate mild impacts, approach at a slower speed, stop near the event, and recover on a bounded timer.
- Repeated equal cues extend location/lifetime without replaying startle; materially stronger cues can interrupt while weaker noise cannot thrash the response.
- Added the shared `PedestrianIntent` contract and kept police pursuit/search tactics outside the civilian reaction system.
- Added authoritative synchronized NPC action intent for `wander`, `startle`, `flee`, `investigate`, `recover`, `pursue`, `search`, and `dead`.
- Added pure client presentation policy: startle/recovery pause and tint, flee/pursuit accelerate animation, and investigation/search slow it. Presentation never advances gameplay state.
- Respawn now clears personal threat, stimulus memory, and reaction state before restoring wander.

### QA

- Added transition scenarios for startle/flee/safe recovery, mild-impact investigation, repeated-cue extension, and police isolation.
- Added renderer policy coverage and extended personal-threat/controller diagnostics coverage.
- Production TypeScript and Vite build: passing; focused reaction/perception/render/debug suite: 24/24 passing.
- Live F3 showed a nearby civilian switch to `startle` with gunshot memory, move away from the danger radius, and return to `wander`; synchronized action presentation rendered in the browser.
- Full suite: 76/76 passing, including the line-of-sight-hardened real two-client scenario.

### Next Analysis

Add a sidewalk/navigation destination layer before increasing crowd complexity. Production behavior chooses reachable flee and investigate destinations; the current local angle steering is adequate for the slice but will still snag on large buildings and cannot coordinate crossings or safe gathering points.

## 08:00 - Longer Danger Response and Bounded Pedestrian Routes

### Research

- Revisited the production separation between pedestrian and vehicle paths, persistent route-node progress, fixed path buffers, nearest-node selection, disabled links, crossings, and deterministic recovery.
- Recorded the clean-room implementation and replacement boundary in `docs/PEDESTRIAN_NAVIGATION_RESEARCH.md`.
- Incorporated direct play feedback that a gunshot response lasted only a few steps and did not visually communicate sustained danger.

### Delivered

- Rebalanced flight with danger-specific minimum commitments: 1.8 seconds for incidental impacts, 3.4 seconds for injury, 4.2 seconds for gunshots/death, and 4.8 seconds for fire/explosion-class danger.
- Increased safe-distance targets and gave each reaction a stable flee destination or investigation goal instead of recalculating a short local movement vector.
- Added a deterministic bounded A* compatibility planner over the current collision map with actor clearance, diagonal corner protection, nearest-open goal handling, stable tie breaking, route smoothing, 384-node expansion cap, and 28-waypoint cap.
- Added private navigation goal, route, waypoint progress, completion, retry, and destination-change invalidation state.
- Limited new pedestrian searches to two per simulation tick; direct paths bypass search and blocked actors retain deterministic local recovery.
- Preserved police target aim independently of route direction.
- Extended opt-in F3 diagnostics with route goals, remaining waypoint lines/points, and path progress labels. Ordinary clients still receive no route state.

### QA

- Added deterministic wall-routing, expansion-budget, per-tick request-budget, sustained-gunshot-flight, route ownership, and debug copy-isolation coverage.
- Focused navigation/reaction/debug suites pass. The first full run passed 77/78 and exposed a timing-sensitive multiplayer kill wait whose firing lane had never been verified.
- Hardened both combat phases to establish authoritative map line of sight, recalculate aim per shot, and stop firing once the target dies. The real two-client scenario then passed in isolation and under the full test load.
- Production TypeScript and Vite build: passing. Final full suite: 78/78 passing.
- Live browser reload remained online with F3 projecting pedestrian objectives, bravery, route-capable labels, 13 NPCs, 11 vehicles, and zero simulation drops/deferred work. No new warning or error was recorded after the clean reload; active blocked-route rendering remains covered by the deterministic wall fixture.

### Next Analysis

Consolidate every requested gameplay, social, economy, customization, property, map, and onchain item into one dependency-ordered status roadmap. Then move to production-referenced vehicle/traffic depth and content variety without embedding economy or persistence rules into the district room.

## 08:06 - Canonical Feature Roadmap

### Delivered

- Added `docs/FEATURE_ROADMAP.md` as the canonical status and dependency list for every requested system.
- Marked current browser multiplayer, weapons, Q/E cycling, passenger drive-by presentation, four-seat cars, hijacking, traffic, nuanced vehicle damage, wanted/police, minimap, Freemode crew mission, pedestrians, navigation, and F3 diagnostics as playable versus merely foundational.
- Ranked the next vertical slices as vehicle/traffic depth, closed-loop street services, reusable mission objectives and event modes, free character creation, then durable identity/economy.
- Preserved Stick RPG stats/jobs/bank, clans/crews, garages, properties, businesses, markets, multiple districts, and GTA Online-style group content as explicit dependency-gated work rather than vague backlog items.
- Integrated the OpenGTA2 Three migration as a parallel renderer track and Robinhood Chain as an optional asynchronous settlement layer after original assets, persistence, anti-abuse, and review gates.
- Replaced the stale immediate sequence in `GAMEPLAY_RESEARCH.md` with current completion status and the next implementation order.

### Next Analysis

Study production vehicle archetype, handling, damage, traffic following/braking, obstacle response, and police-driving ownership before extending the existing vehicle catalog. The first implementation must stay data-driven and reuse authoritative vehicle primitives instead of adding type checks to the room or renderer.

## 08:17 - Shared Vehicle Catalog and Traffic Awareness

### Research

- Reviewed production handling-data ownership, automobile model flags, separate simple/full simulation, autopilot route/lane memory, sector-scanned vehicle/pedestrian danger, asymmetric target-speed changes, link-owned traffic lights, and temporary recovery actions.
- Recorded the clean-room ownership and deferred lane/police/LOD work in `docs/VEHICLE_TRAFFIC_RESEARCH.md`.

### Delivered

- Added one immutable shared vehicle catalog consumed by server and browser adapters rather than duplicated model checks.
- Gave Sedan, Taxi, and Police Cruiser distinct health, mass, collision response, acceleration, braking, speed limits, steering/grip, traffic policy, seating, frame, dimensions, and emergency-light capability.
- Driver input now uses model handling and treats opposite-direction throttle as braking before reversing.
- Vehicle access consumes configured seat capacity; population and mission return-to-traffic consume configured cruise and health values; rendering consumes configured frame/dimensions/lights.
- Added pure `TrafficAwarenessSystem` with bounded dynamic look-ahead, actor-local forward/lateral projection, footprint-aware corridor, closing speed, safe gap, stopping-speed limit, deterministic limiting-obstacle selection, and vehicle/pedestrian reasons.
- Traffic now accelerates and brakes asymmetrically, distinguishes normal following from world blockage, and selects a deterministic alternate road edge after a sustained blocked route.
- Extended opt-in F3 vehicle labels with desired speed, stop/follow reason, obstacle/gap, and recovery count; ordinary clients receive no traffic runtime state.

### QA

- Added catalog completeness/distinction, player acceleration, direction-change braking, irrelevant-obstacle rejection, lead-car following, pedestrian stop, order independence, controller braking, route-heading awareness, no node snap-through, diagnostics, and blocked-route recovery scenarios.
- Production TypeScript and Vite build: passing. Final full suite: 87/87 passing, including the real two-client combat/vehicle/mission/respawn scenario.
- Live browser F3 showed a traffic car at `ai:vehicle target:0 gap:28`, a Taxi at 950/950 health, 13 NPCs, 11 vehicles, zero dropped/deferred simulation work, and no warning/error after a final clean reload.

### Next Analysis

Continue vehicle depth with an authored lane/intersection contract and police-driving intent only after the current road-map metadata is audited; otherwise move to visible street services if the map cannot yet represent safe intersections.

## 08:31 - Idempotent Street Economy Boundary

### Research

- Reviewed production shop-pickup payment/grant/respawn ownership, garage eligibility and timed service states, full vehicle repair/fire reset, condition-based crusher rewards, and GTA2 cash-gated vehicle services.
- Reconciled that gameplay boundary with the project onchain plan: this is temporary non-redeemable street cash, while persistence and optional settlement remain later asynchronous adapters.
- Recorded the current and replacement contracts in `docs/STREET_ECONOMY_RESEARCH.md`.

### Delivered

- Added bounded `StreetEconomyController` as the only current cash mutation owner, with positive safe-integer validation, maximum balance, explicit failures, stable idempotency, fail-closed capacity, bounded copied snapshots, and typed `economy.changed` audit events.
- Successful transaction keys remain reserved for the room lifetime; failed purchases do not consume their key and may retry after funds change.
- Replaced direct player, civilian, and police kill cash mutations in `DamageController` with authoritative tick-keyed economy credits.
- Replaced mission payout mutation and the mission-local payout set with the mission system's existing participant-specific keys through the economy port.
- Mission payout events/notices now occur only after an applied transaction; F3 recent events summarize economy direction, amount, reason, and resulting balance.

### QA

- Added credit/debit, duplicate, retry-after-insufficient-funds, validation, missing-player, balance-cap, capacity, bounded-snapshot, and audit-event scenarios.
- Production TypeScript and Vite build: passing. Full suite: 90/90 passing, including economy, mission idempotency, and the real two-client combat/vehicle/death/respawn scenario.

### Next Analysis

Add the first two visible service stations behind a service controller and interaction-priority adapter: a driver-in-vehicle repair garage and an on-foot ammunition counter. Each must quote/validate/debit first, then apply one complete authoritative effect, expose minimap/world markers and notices, and remain independent of durable persistence.

## 08:48 - Playable Repair and Ammunition Services

### Research

- Revisited production garage containment, eligibility, staged restoration, fire/component reset, payment ordering, pickup availability, and contextual interaction ownership.
- Recorded the clean-room service, interaction, presentation, and persistence replacement contracts in `docs/STREET_SERVICES_RESEARCH.md`.

### Delivered

- Added shared bounded ammunition and layered vehicle-repair quote policy with stable service kinds, capacities, and interaction radii.
- Added deterministic replicated Ammunition and Repair Garage locations with collision-safe placement, world-space service boundaries/labels, and minimap shop markers.
- Added authoritative on-foot restock and driver-in-vehicle repair flows with alive/action/proximity/damage/ammunition, wanted, fire/destruction, speed, and funds validation.
- Successful purchases debit the central street-economy port before dispatching one complete effect to `FireControlController` or `VehicleSimulationController`; rejected purchases preserve cash and domain state.
- Added `PlayerInteractionController` so a valid service takes priority over vehicle access and duplicate interaction messages in one simulation tick cannot trigger either path twice.
- Extracted pure client interaction affordance/minimap policy and one presentation controller for desktop, touch, ARIA, minimap, and world-marker output.
- Replaced the mission-named notice channel with generic `game.notice` transport shared by missions and services.
- Added explicit Vite lifecycle teardown for the Phaser game and Colyseus room, including partial-boot failure cleanup, preventing hot updates from accumulating ghost clients in the active page.

### QA

- Added deterministic service initialization, quote bounds, successful repair/restock, wanted rejection, state preservation, service priority, same-tick deduplication, and client projection tests.
- Extended the real room integration test to require both replicated service kinds.
- Production TypeScript compilation passed; full suite passed 98/98 including the real two-client mission/combat/vehicle/passenger/respawn scenario.
- Live browser QA showed the Ammunition boundary/label and minimap shop point, one Phaser canvas plus the minimap canvas, 13 NPCs, 11 vehicles, online state, and no new warning/error after clean reload and hot update.

### Next Analysis

Build reusable Freemode objective definitions before adding more one-off mission code. Start with production-referenced reach-zone, acquire/steal, escape-heat, delivery, eliminate-target, hold-area, and checkpoint-race contracts; then compose Courier/Getaway and Street Race jobs while preserving roster locks, leader transfer, individual-death tolerance, entity cleanup, and idempotent payouts. Character appearance content can proceed in parallel through stable IDs and replicated palette data without entering the room or changing gameplay statistics.

## 09:07 - Reusable Freemode Objectives and Getaway Run

### Research

- Re-audited the pinned production script runtime, local mission state/timers, reusable vehicle/area/blip/garage commands, bounded mission cleanup, and explicit termination behavior.
- Recorded the clean-room catalog, evaluator, runtime, projection, second-job, and deferred-objective contracts in `docs/MISSION_OBJECTIVE_RESEARCH.md`.

### Delivered

- Added an immutable shared mission catalog with stable template/objective IDs, offer copy, reward/duration/crew policy, ordered objective definitions, validation, and deterministic selection cycling.
- Split condition-sensitive reward math and pure bounded objective evaluation out of `MissionSystem`.
- Added reusable acquire-target, ordered vehicle-checkpoint, clear-team-wanted, and low-speed-delivery predicates. Template advancement is bounded by objective count and ordered checkpoints cannot be skipped.
- Refactored `MissionSystem` around roster, leader, deadline, reservation, objective-progress, payout, failure, and removal ownership instead of hard-coded Boost phase rules.
- Added Getaway Run: steal the target, drive three deterministic road-safe checkpoints, clear crew heat, and deliver for a condition-sensitive $1,100 base payout.
- Replicated stable template/current-objective metadata and only the current checkpoint, keeping future route data private.
- Added a compact desktop/mobile job selector and catalog-driven title, summary, reward, objective copy, world checkpoint zone, and minimap marker presentation.
- Kept `DistrictRoom` to one typed start-message adapter; route generation, objective decisions, economy, vehicles, wanted, and presentation remain separate owners.

### QA

- Added catalog integrity/cycling, objective gate/ordering/speed/wanted, full Getaway progression, collision-safe generated route, schema projection, and second-job room payout scenarios.
- Extended the real two-client integration to verify objective schema while preserving mission, weapons, shared cars, passenger fire, combat, police, death, and respawn.
- Full suite passed 105/105. Production TypeScript and Vite build passed.
- Live browser QA selected Getaway Run, showed its $1,100 offer, started and automatically locked the crew, launched the acquire objective, added the target minimap marker, and cleaned up after abandonment.
- Desktop and 390x844 layouts showed no mission text/control overflow or overlap; no new warning/error appeared during the complete browser pass.

### Next Analysis

Add a third job only if it proves a genuinely new objective boundary: a holdout can establish contested-zone presence, survival timing, combat-event contribution, and respawn-safe completion. In parallel, begin the free character creator foundation with stable appearance IDs/palette values, replicated state, pure composition policy, and no gameplay-stat coupling. Hospital/clothing services should then consume the same appearance/economy ports rather than introducing a separate customization path.

## 09:29 - Replicated Free Character Creator

### Research

- Reviewed production player-skin lookup/default fallback, bounded naming, frontend preview begin/render/end lifecycle, saved preference, resource replacement/deletion, and separation from player physics.
- Verified Rockstar's current distinction between editable appearance/apparel and immutable or deletion-gated identity traits, with access/fee policy owned by the style surface.
- Recorded clean-room catalog, schema, renderer/cache, creator, persistence, monetization, and original-art replacement contracts in `docs/CHARACTER_APPEARANCE_RESEARCH.md`.

### Delivered

- Added a finite shared appearance catalog for body presentation, six skin tones, hair/color, headwear, top/color/accent, bottoms/color, shoes/color, and bounded outfit names.
- Added nested replicated player appearance schema plus a dedicated server controller for invalid-join fallback, atomic full-update validation, 150 ms rate limiting, and disconnect cleanup.
- Added join-time local development preference and live `appearance.update` protocol; every submitted field is a stable catalog ID rather than a filename, URL, CSS value, or arbitrary asset key.
- Added one canvas palette/style renderer shared by creator preview and Phaser world textures. It preserves source shading, applies separate body regions, draws pixel style/headwear details, and creates matching nine-frame walk animations.
- Updated local, remote, on-foot, weapon, and passenger player presentation to consume the selected appearance; visual body width never changes gameplay collision or statistics.
- Added a 96-entry inactive texture/animation pruning boundary against valid-value GPU cache growth.
- Added the `LOOK` creator with menus, target tabs, ten color swatches, outfit naming, randomize, cancel, apply, preview, desktop/mobile layout, and local reload persistence.
- Opening the modal stops movement immediately and blocks movement, aim, fire, weapon cycling, interaction, and mission controls until close.

### QA

- Added catalog/name/ID/texture-key, palette-zone/body-scale, server fallback/update/rate-limit/rejection/no-partial-mutation, and disconnect-cleanup coverage.
- Extended real two-client integration with join-time appearance and live remote update replication while preserving every existing mission/combat/vehicle/passenger/respawn behavior.
- Full suite passed 111/111. Production TypeScript and Vite build passed.
- Live browser QA applied a broad/deep/curls/cap/hoodie/track/boots look with distinct hair/top/bottom colors, verified world replacement, creator reopen, and reload persistence.
- Desktop creator fit at 680x523. The first 390x844 pass exposed an invalid underscored CSS z-index; correcting it isolated the modal above HUD/touch layers, and the final 366x610 mobile panel had no overflow or overlap.
- No new browser warning/error appeared during apply, schema update, hot reload, persistence reload, or responsive QA.

### Next Analysis

Use the new appearance/economy boundaries for a clothing preview/store service only after defining owned-versus-equipped inventory and purchase rollback. The next immediate gameplay slice should establish a genuinely different hold-area/survival objective and one Holdout Freemode job, including contribution time, contested presence, respawn tolerance, police/combat event inputs, and payout eligibility without importing damage internals into missions.

## 09:47 - Target-Free Crew Checkpoint Mission

### Research

- Reviewed production generation-checked script spheres, explicit marker removal, player any-means/on-foot/in-car/stopped predicates, ordered race pickups, closest road-node placement, and independent timer/counter presentation.
- Recorded the clean-room target-policy, participant-snapshot, route, projection, cleanup, and next Holdout boundary in `docs/CREW_CHECKPOINT_RESEARCH.md`.

### Delivered

- Added template-declared `targetMode` and `rewardPolicy`, removing the assumption that every Freemode job reserves a traffic vehicle and earns condition-sensitive payout.
- Added a pure `crew-checkpoints` objective that advances only when a connected living participant in an authoritative existing vehicle reaches the current ordered zone.
- Updated participant snapshots to use effective vehicle coordinates, preserving passenger/support completion while rejecting nonexistent vehicle IDs.
- Added Crew Checkpoint Rush: five deterministic road-safe checkpoints, 150-second timer, shared arbitrary-crew-vehicle progress, no target/delivery entity, and fixed $900 participant payout.
- Preserved roster lock, leader transfer, individual-death tolerance, timeout, abandonment, idempotent economy credit, terminal retention, and entity cleanup through the existing runtime.
- Added catalog-driven HUD/objective copy, current checkpoint world/minimap projection, and generalized the stale `boost-contact` marker identity to `freemode-contact`.
- Corrected mission selector grid placement after live QA exposed active titles collapsing to `C...`; the full title now has 218 px available on desktop and 136 px on 390 px mobile without overflow.

### QA

- Added target/reward catalog, alive/connected/in-vehicle predicate, out-of-order rejection, missing-target immunity, fixed payout, no-empty-reservation, room-generated route, effective vehicle position, target-free HUD, and minimap coverage.
- Focused mission suite: 19/19 passing. Full suite: 115/115 passing, including real two-client combat, passenger shooting, vehicles, mission flow, appearance replication, death, and respawn.
- Production TypeScript and Vite build passed.
- Live desktop and 390x844 QA selected, started, launched, and abandoned Crew Checkpoint Rush; the HUD showed checkpoint 1/5 and $900, the minimap added exactly one objective marker, and no target marker appeared.
- Measured mobile HUD bounds: all mission text/control scroll dimensions fit their clients, the weapon panel retained a six-pixel gap, and the temporary viewport override was reset.
- A clean reload after the complete pass produced zero new warning/error logs.

### Next Analysis

Implement Holdout only after defining production-grade wave ownership and combat-event input: spawn budget, hostile archetype/weapon policy, contested-zone presence, contribution time, death/respawn behavior, terminal cleanup, and anti-idle payout eligibility. Then return to the canonical queue: police road pursuits/intersection ownership, hospital and clothing services, weapon/explosion depth, durable identity/ledger, crews/clans, garages/property, markets/businesses, multiple districts, and only then optional asynchronous onchain settlement.

## 10:16 - Event-Driven Crew Holdout

### Research

- Reviewed production mission-ped creation/marking, bounded cleanup, pedestrian kill-objective assignment, kill-frenzy target/timer/pass/fail state, temporary loadout restoration, and player-death policy.
- Reviewed Rockstar's Survival Creator and Survival descriptions for authored spawn points, combat proficiency, vehicles/props, crew waves, finite progression, and between-round loadout opportunities.
- Recorded the clean-room encounter, mission actor, hostile combat, hold/contribution, presentation, cleanup, and deferred tactical-content contracts in `docs/HOLDOUT_ENCOUNTER_RESEARCH.md`.

### Delivered

- Added `MissionEncounterSystem` with immutable bounded wave definitions, at most one spawn per update, explicit spawn/inter-wave cadence, active/pending actor ownership, nearest living roster target assignment, event-driven defeat contribution, contest snapshots, and deterministic cleanup IDs.
- Added mission-only hostile pedestrian lifecycle plus a dedicated `PedestrianCombatSystem` for line-of-sight pursuit, navigation targets, stop distance, aim, and configured fire cadence.
- Added hostile projectile ownership and orange projectile presentation. Hostile attacks can damage players/vehicles and preserve attacker identity, but do not create street crime, wanted heat, kill cash, panic, or ambient respawn.
- Added pure hold-area progress: living crew presence required, one-second update cap, pause while contested, survival across individual death/respawn, and completion only when defended time and waves are both complete.
- Added per-participant in-zone contribution and a five-second Holdout payout threshold, excluding idle roster members while preserving shared completion.
- Added Crew Holdout: 2/3/4 attackers across pistol/pistol/SMG waves, 60/75/90 health, 25 defended seconds, 180-second deadline, and fixed idempotent $1,200 payout.
- Added replicated hold/wave/remaining/contest state, blue/red world boundary, hostile actor/minimap presentation, compact mobile HUD, F3 hostile target/fire events, and F3 hold/wave labels.
- Completion, timeout, abandonment, and cleanup clear targets and despawn every mission actor immediately; dead hostiles never ambient-respawn.

### QA

- Added encounter wave/target/contribution/cleanup, hold presence/contest/timer/completion, idle payout exclusion, hostile combat/fire/non-respawn/despawn, no-crime/no-cash hostile damage, projectile ownership, rendering, catalog, room, HUD/world/minimap, and all-nine-actor completion scenarios.
- Focused implementation suite passed 42/42. Full suite passed 122/122, including the real two-client mission/combat/shared-car/passenger-fire/appearance/death/respawn scenario. Production TypeScript and Vite build passed.
- Live desktop QA showed two red pistol attackers navigate and fire, damage a nearby car, kill the player without heat, and leave the Holdout active through death. F3 showed hostile owner IDs, targets, weapon events, damage, kill, and zero incidents/pursuits.
- Live 390x844 QA showed the full Holdout title/objective, `W1/3 | 2 LEFT | $1200`, action, boundary, minimap blips, and death presentation with no text/control overflow.
- Abandonment immediately removed hostile blips/actors. The viewport override was reset and the complete clean pass produced zero new warning/error logs.

### Next Analysis

Return to the canonical non-mission queue. First audit the map's road metadata against production police vehicle pursuit, intersection/lane ownership, disabled-car avoidance, and spawn/despawn policy; if the compatibility road graph cannot support safe pursuit yet, implement the hospital/clothing service and owned-versus-equipped inventory boundary before durable persistence. Weapon/explosion depth follows those slices, then identity/ledger, crews/clans, garages/property, markets/businesses, multiple districts, and optional asynchronous onchain settlement.

## 10:46 - Report-Based Police Cruiser Pursuit

### Research

- Reviewed pinned production `AutoPilot`, `CarAI`, `CarCtrl`, and `RoadBlocks` ownership: car mission/strategy, wanted-scaled speed, far/close transitions, route/link execution, temporary recovery, and roadblock planning remain separate concerns.
- Audited the compatibility roads layer: 1,861 road cells, five components, and 94.4% in the largest component. The broad high-degree mask can support a first bounded pursuit but not lane direction, legal turns, stop lines, signals, parking, or coordinated roadblocks.
- Confirmed OpenGTA2 contributes map/junction parsing but no production police driving runtime. Recorded source findings, clean-room boundaries, strategy policy, QA contract, and lane-data gate in `docs/POLICE_VEHICLE_PURSUIT_RESEARCH.md`.

### Delivered

- Added deterministic visit-bounded A* with complete/partial route results, stable tie-breaking, nearest valid road-node lookup, and a reusable road-point projection.
- Extracted `RoadDrivingSystem` below ambient policy so traffic and police share road occupancy, turn rate, acceleration/braking, ahead-corridor obstacle awareness, and controlled off-road rejoin without sharing strategy.
- Added `PoliceVehicleDispatchSystem`, pure cruiser policy, and `PoliceVehicleController` as separate assignment, decision, and orchestration owners. Crime response supplies reported/current perception facts; ambient traffic never reads wanted state and the cruiser never mutates incidents, damage, or player control.
- Added report-position search, line-of-sight observation, nine-second last-known expiry, newer-report redispatch, bounded 650 ms replanning, visible close interception, occupied-vehicle lead, heat-scaled speed, and heat-3+ ramming policy.
- Added a validated on-road police cruiser response unit, pursuit-only replicated siren lights, immediate hijack yield, ordinary player handling after entry, destruction cleanup, and active-cruiser suppression of wanted decay.
- Added F3 cruiser strategy/target/speed/obstacle labels, capped route and last-known overlays, visited/complete diagnostics, and a global `Cruisers active/total strategy` field for off-camera verification.

### QA

- Added deterministic shortest/partial-route, response priority/caps/new-report, and police search/visibility/ram/expiry/hijack scenarios. Focused route/police/traffic/debug suite passed 12/12; the final full suite passed 128/128, including the real two-client mission/combat/shared-car/passenger-fire/appearance/death/respawn scenario.
- Production TypeScript and Vite build passed after simulation, schema, client siren, diagnostics, and panel changes.
- Live QA used a real second Colyseus client through ordinary movement, aim, shooting, witness, vehicle-entry, and driving commands. The browser observed heat 2, two/three replicated players during overlap, one foot pursuit, `Cruisers 1/1 intercept`, an extra minimap player marker, and authoritative police projectile damage/ignition on the getaway car.
- After suspect death/disconnect, foot pursuit and cruiser assignment cleared and F3 returned to `Cruisers 0/1 idle`; no pursuit state leaked into respawn.
- Mobile 390x844 kept the added diagnostics inside the existing scrollable panel. The viewport was reset, and a settled desktop reload produced zero new warning/error logs.

### Next Analysis

The user feature list remains canonical and dependency-ordered. Lane/intersection metadata is the next prerequisite for deeper traffic, multi-cruiser blocking, roadblocks, and siren yielding. The next independent playable slice should close the service/economy loop with production-researched hospital respawn choices and a clothing preview/store boundary that distinguishes owned from equipped items without introducing persistence or monetization yet. Weapon pickups/explosions, additional vehicle types, mission objective depth, durable identity/ledger, crews/clans, garages/property, markets/businesses, multiple districts, and asynchronous onchain settlement remain queued in `FEATURE_ROADMAP.md`.

## 11:19 - Hospital Care and Protected Respawns

### Research

- Reviewed pinned production `GameLogic`, `Restart`, `PlayerInfo`, `PlayerSkin`, and frontend ownership for death state, hospital selection, one-time consequences, preview/apply/cancel, catalog validation, fallback, and resource lifecycle.
- Recorded the multiplayer adaptation and medical/wardrobe separation in `docs/MEDICAL_WARDROBE_RESEARCH.md`: public care must be nonblocking, trauma is an optional faster sink, equipped appearance is public, and future wardrobe ownership stays private.

### Delivered

- Added two deterministic collision-safe hospitals with shared content definitions, replicated world/minimap presentation, contextual treatment actions, and nearest-facility respawn selection.
- Added `MedicalCareController` as the sole owner of private admissions, care choice, timing, facility selection, living treatment, and idempotent economy coordination.
- Added free 4.2-second Public Ward care that preserves remaining ammunition and $250 2.2-second Trauma Care that restores ammunition, preventing free ammo refill exploits while retaining a nonblocking fallback.
- Kept death/respawn mutation in `PlayerLifecycleController`, hospital routing in `StreetServiceController`, damage authority in `DamageController`, successful-shot cancellation in `FireControlController`, and network wiring in `DistrictRoom`.
- Added a three-second server-authoritative spawn shield after live QA exposed immediate re-death near Holdout attackers. Firing cancels it, expiry is private and explicit, replication is one boolean, and presentation uses a cyan pulse plus F3 `SHIELD` label.
- Added a compact death choice surface with affordability/selection state and one medical protocol command; insufficient Trauma funds preserve the public fallback without consuming a debit key.

### QA

- Added deterministic facility/admission/debit/treatment, UI policy, public ammo preservation, trauma refill, damage protection/expiry, shot cancellation, service projection, and integration coverage.
- Full suite passed 133/133, including the real two-client weapons, shared vehicle, passenger-fire, appearance, mission, death, and respawn scenario. Production TypeScript and Vite build passed.
- Live Holdout QA reproduced the former spawn-kill loop, showed Public Ward and disabled unaffordable Trauma Care, returned at full health, preserved bounded protection, and allowed ordinary damage again after expiry.
- Desktop buttons measured 112x38 each inside a 232 px nonoverflowing group. The requested 390x844 browser override read back as 1280x720, so true mobile breakpoint measurement is recorded as pending rather than falsely passed. A clean settled reload produced zero fresh warning/error logs.

### Next Analysis

Implement the researched wardrobe boundary next: private development grants, owned-item validation, equipped-only public replication, explicit preview/apply/cancel rollback, and a clothing service that opens the existing creator. Then return to authored lane/intersection metadata and coordinated traffic/police depth, followed by weapon pickups/explosions, more validated vehicle types, deeper mission objectives, durable identity/ledger, crews/clans, garages/property, markets/businesses, multiple districts, and only then optional asynchronous onchain settlement.

## 11:35 - Private Wardrobe and Clothing Store

### Research

- Rechecked pinned production frontend/player-skin behavior for bounded catalog enumeration, separate preview and use actions, prior-state restoration, validated fallback, and explicit preview resource begin/end lifecycle.
- Audited the current browser creator and found one authority gap: it persisted locally before the server accepted an update. Defined private owned-versus-equipped state and an acknowledgement contract before adding store presentation.

### Delivered

- Added a finite namespaced wardrobe catalog for every hair, headwear, top, bottom, and shoe style, plus explicit baseline and all-content development grants.
- Added `WardrobeInventoryController` with private per-player sets, deterministic owner snapshots, missing-item checks, grants, and disconnect cleanup. No inventory field was added to public schema.
- Added server-side entitlement checks to `PlayerAppearanceController`; unowned requests fail without partial mutation or consuming the update cooldown.
- Added owner-targeted wardrobe request/state messages and an authoritative appearance result. Extracted `WardrobeClientSession` to own subscriptions, inventory, store-open messages, one in-flight apply, and result delivery outside the DOM controller.
- Added collision-safe `Threads` near spawn, wanted/vehicle gates, a pink hanger marker, `BROWSE LOOKS` action, and store-open messaging that reuses the existing creator as `WARDROBE`.
- Kept every current option freely granted and retained the global `LOOK` button as requested. This establishes future ownership/purchase ports without introducing pricing or monetization.

### QA

- Added catalog mapping/uniqueness, baseline/development grants, private inventory separation, missing/grant/equip behavior, unowned appearance rejection, cadence preservation, service placement/discovery, wanted/vehicle gates, and presentation policy tests.
- Full suite passed 138/138 after the client-session extraction. The real two-client scenario proves owner-only wardrobe delivery, all development grants, appearance acknowledgement, and every existing combat/vehicle/mission/medical behavior. Production TypeScript and Vite build passed.
- Live browser QA opened `WARDROBE`, verified zero disabled options, applied `Store Fit` with a beanie, and reopened the store to verify authoritative state.
- The first at-spawn placement exposed service-priority starvation of car entry in the full regression. The final placement is 100-120 px from spawn, outside its 76 px radius; the dedicated store scenario and real two-client vehicle flow both pass.
- Direct `LOOK` apply/reopen also passed. The creator measured 680x523 with matching client/scroll dimensions, and a settled reload produced zero fresh warning/error logs.

### Next Analysis

Authored lane/intersection metadata remains the prerequisite for safe multi-cruiser roadblocks and signal-aware traffic. While that content track advances, the next independent gameplay slice is a production-reference audit of weapon pickups, thrown/explosive weapons, blast falloff, vehicle/pedestrian/world damage, fire lifecycle, attribution, chain reactions, cleanup, and multiplayer abuse limits before adding any new weapon type.

## 12:24 - Grenades, Shared Pickup, and Vehicle Chain Explosions

### Research

- Reviewed pinned production `Weapon`, `ProjectileInfo`, `Explosion`, `World::TriggerExplosion`, `Fire`, and `Pickups` ownership before implementation.
- Preserved the production separation between weapon dispatch, bounded thrown runtime, one-shot typed blast damage, persistent fire, pickup lifecycle, and presentation. OpenGTA2 remains a compatibility-data reference rather than a gameplay implementation.
- Recorded source behavior, clean-room policy, domain owners, abuse limits, deferred fire/Molotov scope, and QA gate in `docs/EXPLOSIVES_PICKUPS_RESEARCH.md`.

### Delivered

- Added grenade as a fourth authoritative weapon with finite ammunition, two starter rounds, six capacity, cooldown, no passenger use, original icon/model, HUD support, and existing Q/E/button/wheel cycling.
- Added `ThrownProjectileController` with 24 global/two-per-owner caps, private velocity, replicated pose/height/fuse, gravity, wall/ground bounce, damping, exact-once detonation, and deferred collection removal.
- Added `ExplosionController` with typed transient effects, bounded visual capacity, production-shaped inner/outer radial falloff, distinct pedestrian/vehicle scales, spawn protection, self damage, disconnected-source suppression, and exact-once player/NPC/vehicle resolution.
- Adapted `vehicle.destroyed` events into later-step car explosions and retained occupant IDs so destruction/ejection handles occupants once without a second chain-blast hit. Blast direction now damages the correct vehicle component side.
- Added `explosion` as an explicit vehicle damage source and made explosion stimuli originate only from `explosion.created`, avoiding duplicate destruction/explosion panic facts.
- Added `WeaponPickupController` with collision-safe placement, spatial candidate query, nearest/ID-stable contention, shared availability, three-grenade grant, six cap, 20-second respawn, targeted notice, typed event, world label/model, and minimap point.
- Added dedicated client renderers/policies for grenade arc/shadow/fuse pulse, edge-triggered blast rings/core/particles/shake, and pickup presentation. `DistrictRoom` and `DistrictScene` remain wiring/scheduling coordinators.
- Moved event draining/adaptation inside each fixed simulation step so car explosions and mission/pedestrian event consumers retain deterministic step order during catch-up.

### QA

- Added falloff boundary, throw capacity/fuse/bounce/exact-once, blast victim/falloff/occupant/disconnect/chain, self-reward suppression, pickup contention/cap/respawn, fire gate, HUD/model/minimap, and nonduplicate stimulus coverage.
- Focused explosive and presentation suite passed 25/25. Final full suite passed 147/147, including the real two-client missions, weapons, shared vehicle, passenger fire, appearance, death, and respawn scenario.
- Production TypeScript and Vite build passed. `git diff --check` is required immediately before commit.
- Live browser QA showed the shared `GRENADES x3` cache and minimap marker, selected `GRENADE 2`, consumed one authoritative round, observed the replicated transient count move `0 -> 1 -> 0`, and returned to a settled complete map.
- Historical hot-reload console entries were excluded with a timestamp baseline. A fresh reload, grenade cycle/throw/cleanup pass produced zero new warning/error entries.

### Next Analysis

Use the same production-reference rule before the next slice. The highest-value independent choice is either authored lane/intersection metadata for signal-aware traffic and coordinated police response, or a distinct weapon family that proves a new lifecycle. Molotov requires the separate bounded persistent-fire owner documented here; rockets require collision detonation and vehicle/world blast tuning. Durable identity still gates crews, garages, property, markets, and the onchain settlement boundary.
