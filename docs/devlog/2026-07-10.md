# Development Log - 2026-07-10

Timezone: America/Mexico_City (CST)

## 04:01 - Deterministic Simulation and Debug Checkpoint

### Delivered

- Added a fixed-step 30 Hz simulation clock with bounded catch-up and dropped-time accounting.
- Replaced gameplay randomness with named, keyed deterministic samples.
- Added an incremental spatial index for players, NPCs, and vehicles.
- Routed bullet candidates, vehicle impacts, vehicle interaction, and nearby-police checks through the spatial broad phase while retaining exact geometry checks.
- Added deferred lifecycle commands so bullet deletion no longer mutates the collection being iterated.
- Added typed damage, death, crime, and respawn events.
- Added a shared debug snapshot protocol and periodic server snapshots.
- Added a `DBG` / `F3` overlay with collision tiles, spatial cells, entity radii, headings, IDs, health, seat/vehicle state, counts, timing, and recent gameplay events.
- Added original player, civilian, and police sprite sheets required by the active client paths.

### QA

- `npm test`: 8/8 passing, including two-client driving, passengers, shooting, death, respawn, and debug snapshot invariants.
- `npm run build`: passing production TypeScript and Vite build.
- `git diff --check`: clean.
- Live server health: HTTP 200.
- Browser QA at the default mobile-sized viewport and 1440x900 desktop.
- Live invariant observed: spatial entity count equals players + NPCs + vehicles.
- Live event feed observed: heat changes, police damage, player death, and respawn.
- Live simulation observed with zero dropped time and zero deferred commands left after the tick.

### Architecture

- Added `server/game/README.md` for simulation dependency and ownership rules.
- Added ADR 0001 for deterministic time, randomness, spatial queries, lifecycle mutations, and event contracts.
- Added `docs/RE3_AI_RESEARCH.md` with a clean-room mapping from re3 behavior to NOCK0 systems.

### Next Analysis

The next player-visible slice should build on the event boundary rather than add more direct rules to `DistrictRoom`:

1. incident registry and witness reporting;
2. per-suspect heat and last-known police search positions;
3. bounded district dispatch and pursuit assignment;
4. visible police/search diagnostics in the debug overlay;
5. a small repeatable crime-to-escape gameplay scenario.

## 04:15 - Witnessed Crime and Police Dispatch Checkpoint

### Delivered

- Added data-driven crime severities for theft, assault, hit-and-run, murder, and police variants.
- Added a bounded incident registry with deduplication, report scheduling, retention, and expiry.
- Added deterministic witness selection using NPC type, distance, hearing range, and line of sight.
- Changed wanted heat so a crime must be reported before stars appear; unwitnessed incidents expire.
- Added cumulative heat thresholds, delayed decay, and complete suspect reset on death, respawn, or disconnect.
- Added stable police dispatch assignments capped by wanted level.
- Replaced omniscient police tracking with sight-based pursuit and expiring last-known-position search.
- Added incident/report/dispatch events suitable for future audit, mission, economy, and persistence consumers.
- Extended the `DBG` / `F3` view with incident markers, pursuit/search lines, and live counters.

### QA

- Added five headless scenario tests for incident, witness, wanted, dispatch, and pursuit behavior.
- Full test suite: 13/13 passing before the final browser and integration assertions.
- Production TypeScript and Vite build: passing.
- Live browser panel verified with the upgraded debug protocol and zero dropped simulation time.

### Architecture

- Added ADR 0002: extract one domain at a time and keep `DistrictRoom` as a coordinator.
- Kept every new police module independent of Colyseus and Phaser.
- Preserved the future economy boundary: gameplay emits typed facts; persistence and onchain settlement remain asynchronous consumers.

### Next Analysis

Vehicle collision and health should be the next domain because driving, traffic, police chases, theft missions, repair shops, and cash sinks all depend on it. The slice should include vehicle-to-vehicle contacts, impact cooldowns, health, disabled/destroyed states, occupant consequences, repair, diagnostics, and deterministic tests.
