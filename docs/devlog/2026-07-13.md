# 2026-07-13

## 22:21 CST - Removed Phaser Renderer and Privy Auth

- Deleted the Phaser client: `DistrictScene`, eleven Phaser `*-renderer.ts` wrappers, the Phaser-specific input/camera/mission/interaction/debug presentation controllers, and the Phaser appearance texture factory. The Three.js client is now the only renderer and boots unconditionally; the `?renderer=` flag is gone.
- All renderer-neutral `*-policy.ts` modules, shared protocol/content, and every server system are untouched. 298/298 tests pass, `tsc --noEmit` is clean, and `next build` succeeds.
- Removed the Privy integration for now: `PrivyProvider`, `useNockPrivySession`, the onboarding email/code login panel, the profile popup, and server-side token verification (`server/auth/privy-auth.ts` replaced by a guest-only `client-auth.ts`). Clients always join as guests. `shared/protocol/auth.ts` keeps the `privy` payload variant so the protocol seam survives for a future auth return.
- Dropped `phaser` and all `@privy-io/*` packages from dependencies.
- Verified in a real Chrome production-build session: guest onboarding, Three district geometry, HUD, minimap, F3 diagnostics with live player/NPC/vehicle counts, mission offer panel, and weapon pickups all render and replicate.
- Found pre-existing issue (unrelated to this change, reproduced on unmodified main): the Next dev server (`next dev` + Turbopack) never fires the client boot effect and the HMR websocket fails, so the game stalls at the loading screen in dev. The production build (`next build` + `next start`) works. Needs its own diagnosis.
