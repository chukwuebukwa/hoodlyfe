# Devlog - 2026-07-13

## 07:38 CST - Interaction-Island Netcode Foundation

- Created `codex/interaction-island-netcode` from clean, current `origin/main` at
  `e48c622`.
- Added a shared pure vehicle simulation kernel for fixed-step handling, finite input
  normalization, damaged-engine speed limits, integration, and swept static collision.
- Replaced duplicated authoritative and browser vehicle handling with the shared kernel.
- Wired replicated `engineDamage` and `onFire` state into Phaser and Three saved-input
  prediction.
- Preserved server ownership of damage, events, occupants, AI traffic, and dynamic
  contacts.
- Replicated `serverTick` and added current, mean, and p95 correction diagnostics plus
  correction counts to the network debug projection.
- Added 10,000-step browser adapter parity coverage and 3,000 authoritative controller
  parity steps across all five vehicle models.
- Added the milestone and rollout plan in
  `docs/INTERACTION_ISLAND_IMPLEMENTATION_PLAN.md`.

QA completed:

- focused netcode and vehicle suite: 21 tests passed;
- complete repository suite: 301 tests passed;
- TypeScript type checking passed;
- optimized Next production build passed.

## 08:12 CST - Three Runtime Gate

- Loaded the live Three renderer at `http://localhost:5173/?renderer=three` against the
  local district server.
- Confirmed the world, local player, vehicles, mission UI, HUD, and minimap render
  together with no runtime exceptions.
- Confirmed all three canvases are visible: a 2560x1440 main backing surface rendered at
  1280x720, plus the minimap and HUD canvases.
- Captured the live main view and measured an 81.63% nonblack-pixel ratio and a 15.71%
  chromatic-pixel ratio; the center pixel was nonblack. This guards against the previous
  blank-canvas failure mode.
- Removed the now-unused server-local angle normalizer after shared-kernel extraction.

QA completed:

- Three runtime smoke passed at desktop viewport;
- browser console contained no application runtime errors;
- source diff whitespace validation passed.

## 08:34 CST - M1 Protocol Contract Checkpoint

- Added versioned wire contracts for unified player commands, applied-input receipts,
  immutable interaction snapshots, typed physical entity baselines, and last-applied
  remote intent.
- Added bounded command admission for monotonic sequence windows, client prediction
  ticks, controlled-entity identity, finite/clamped movement, normalized aim, known
  button bits, weapon slots, and provisional spawn IDs.
- Added fail-closed snapshot admission for server tick age, protocol and static-collision
  compatibility, entity capacity, lifecycle/collider revisions, finite physical state,
  stable identities, one space/layer, and intent-to-entity referential integrity.
- Runtime accepted values are reconstructed from validated fields and deeply frozen at
  the snapshot, collection, entity, intent, command, and spawn-ID boundaries.
- Split the implementation into wire contracts, command validation, snapshot validation,
  shared primitives, and a public barrel; no policy was added to `DistrictRoom`.
- Registered the complete M0-M11 interaction-island rollout as the active development
  goal. Each checkpoint will continue to record implementation and QA here.

QA status:

- protocol and deterministic vehicle tests: 7 passed;
- TypeScript type checking passed;
- the first aggregate run reported a transient failure in the existing two-client
  integration suite; a reporter-isolated rerun completed all 305 tests, including the
  19.6-second live multiplayer scenario;
- optimized Next production build and TypeScript validation passed;
- M1's fail-closed command/snapshot contract gate is complete.

## 08:52 CST - M0 Deterministic Network Gate

- Added four repeatable WebSocket-oriented impairment profiles: local, 75 ms regional,
  150 ms continental, and 250 ms intercontinental RTT with increasing jitter and
  0-1% simulated packet loss.
- Modeled packet loss as ordered retransmission and head-of-line delay because live game
  commands use reliable WebSockets; the harness does not silently delete commands as a
  UDP transport would.
- The scripted 12-second drive passes through real command admission, the shared vehicle
  kernel, immutable snapshot admission, saved-input reconciliation, and retained history.
- Added the reusable `npm run test:netcode` gate.
- The harness exposed and fixed command-level vector normalization: steering and throttle
  are now clamped independently at the protocol boundary, while on-foot vector
  normalization remains family-kernel policy.

Measured deterministic baseline:

| Profile | p95 correction | Maximum correction | Maximum pending | Resimulations | Retransmits |
|---|---:|---:|---:|---:|---:|
| Local | 0.00 px | 0.00 px | 0 | 0 | 0 |
| 75 ms | 13.67 px | 13.89 px | 6 | 55 | 1 |
| 150 ms | 13.67 px | 81.53 px | 9 | 74 | 2 |
| 250 ms | 77.00 px | 108.35 px | 17 | 93 | 4 |

All profiles remain below the 24-tick/800 ms retained-history budget and the 180 px
hard-correction threshold.

Final QA completed at 09:02 CST:

- dedicated netcode gate: 15 tests passed;
- complete repository suite: 307 tests passed;
- optimized Next production build and TypeScript validation passed;
- source diff whitespace validation passed;
- M0 is complete and ready for its remote checkpoint.

## 10:24 CST - M3 Saved On-Foot Prediction

- Replaced the presentation-only local movement replay with a 30 Hz saved-input
  predictor shared by Phaser and Three.
- Added a pure shared on-foot kernel for finite input normalization, analog/diagonal
  movement, melee locomotion policy, and independent-axis street/interior collision.
- Added bounded batched `on-foot.input` commands with monotonic sequences, a 24-command
  authoritative queue, and acknowledgements only after the server applies a move.
- Retained the legacy held-input path for vehicle and older-client compatibility without
  letting receipt masquerade as authoritative application.
- Added 96 moves of client history, baseline restore and pending-input replay, and hard
  correction for space transitions or errors above 120 px.
- Made the predicted body transform the single source for the local sprite, held weapon,
  nameplate, and debug collider in both renderers. This removes the floating-weapon and
  trailing-collider split caused by mixing predicted and replicated transforms.
- Kept interior entry/exit server-authoritative while sharing occupancy collision within
  the current space.
- Extended the network diagnostics with on-foot acknowledgement, pending-history,
  correction, and replay metrics; the debug panel now labels vehicle and foot values
  separately.
- Expanded `npm run test:netcode` to include the on-foot kernel, saved predictor, and
  deterministic impairment gate.

Measured on-foot impairment baseline:

| Profile | p95 correction | Maximum correction | Maximum pending | Maximum resimulated moves |
|---|---:|---:|---:|---:|
| Local | 0.00 px | 0.00 px | 0 | 0 |
| 75 ms | 0.00 px | 6.33 px | 8 | 1 |
| 150 ms | 0.00 px | 31.67 px | 10 | 1 |
| 250 ms | 0.00 px | 57.00 px | 23 | 3 |

QA completed:

- exact shared-kernel/authoritative parity over 10,000 movement ticks;
- no predicted wall crossing and no hard correction in all four impairment profiles;
- dedicated netcode gate: 23 tests passed;
- live two-client Colyseus integration passed with sequenced batch application,
  authoritative acknowledgement, and remote position replication;
- complete repository suite: 314 tests passed;
- optimized Next production build and TypeScript validation passed;
- local district server remained healthy on `http://localhost:2567`.

The in-app browser could not attach a fresh test tab during this checkpoint, so no new
visual smoke pass is claimed. The live protocol path was instead exercised through the
real server and two Colyseus clients; the browser attachment limitation remains a QA
environment issue, not a game-runtime pass.

## 11:54 CST - M4 Adaptive Remote Timelines

- Replaced the player-only arrival-time snapshot buffer with one bounded timestamped
  timeline for remote players, pedestrians, and vehicles in both Phaser and Three.
- Render sampling now targets estimated server time minus an adaptive 75-250 ms delay
  derived from p95 patch cadence, p95 jitter, median RTT, and p95 RTT variation.
- Added shortest-path angle interpolation, exact vehicle velocity projection, bounded
  100 ms extrapolation, speed and angular-velocity caps, hostile-value rejection, and
  history reset across teleports or long snapshot gaps.
- Centralized family policy for player, NPC, and vehicle teleport and extrapolation
  limits instead of embedding tuning in either renderer.
- Kept the local driver and local passenger vehicle paths off the remote timeline so a
  passenger camera, body attachment, weapon, and nameplate do not inherit network delay.
- Made each sampled body transform the shared anchor for remote body rotation, weapon,
  nameplate, passenger presentation, blood, fire, and vehicle effects in Three.
- Extended network quality diagnostics with p95 remote snapshot age, buffer underrun
  percentage, and extrapolation percentage.
- Removed the superseded `MotionSnapshotBuffer` implementation and tests.

Measured deterministic timeline baseline:

| Profile | Delay | p95 error | Maximum error | Underrun | Extrapolated |
|---|---:|---:|---:|---:|---:|
| Local | 100 ms | 0.03 px | 0.03 px | 0.0% | 0.0% |
| 75 ms | 100 ms | 0.03 px | 0.03 px | 0.0% | 0.7% |
| 150 ms | 115 ms | 0.02 px | 18.35 px | 0.6% | 37.6% |
| 250 ms | 195 ms | 0.03 px | 34.02 px | 1.7% | 21.3% |

QA completed:

- dedicated deterministic netcode gate: 29 tests passed;
- complete repository suite: 318 tests passed with no failures;
- optimized Next production build and TypeScript validation passed;
- source diff whitespace validation passed;
- live web and district health endpoints both returned HTTP 200.

The in-app browser again timed out while attaching a fresh local tab, so no new visual
smoke pass is claimed for M4. The executable test, build, live HTTP, and district-server
gates passed; visual browser attachment remains a tooling limitation.

## 12:44 CST - M5 Same-Tick Interaction Snapshot Projection

- Added a modular server-side interaction projector beside replication AOI ownership.
  It captures one immutable authoritative frame after fixed-step simulation, deferred
  lifecycle mutation, event observation, and audio publication, then projects client
  baselines during `onBeforePatch()` without rereading mutable world state.
- Added deterministic broad-phase candidate collection for players, pedestrians,
  vehicles, rockets, and thrown projectiles. It filters the local root, occupied player
  bodies, stale records, duplicate identities, mixed spaces, and candidates outside the
  temporary 768 px M5 admission radius.
- Made the locally controlled physical root the first baseline entity. Drivers and
  passengers root on their occupied vehicle; ordinary on-foot players root on their
  humanoid state. A candidate can only be published when its complete state exists in
  the same captured server tick, space, and collision layer.
- Projected exact vehicle/projectile velocity where controller runtime owns it and
  derived humanoid velocity and angular velocity from consecutive captures. Added
  monotonic lifecycle revisions for replacement, reappearance, and reactivation, plus
  collider revisions for physical-shape changes. Revision tracks older than the replay
  window are pruned so long-running ambient population churn cannot grow memory without
  bound.
- Included only known server-applied on-foot and driver intent. Pedestrian behavior and
  traffic route plans remain private server policy and are not copied into replay data.
- Pinned validated per-player snapshot history for 24 ticks / 800 ms and added a shared
  static-world collision revision. Extracted pedestrian radius into a small config
  module so snapshot networking does not import the complete AI controller.
- Added a renderer-independent client inbox that registers the wire message immediately,
  validates every snapshot, retains a bounded ordered history, replaces duplicate ticks,
  counts rejection reasons, exposes subscriptions for M6/M7, and tears down cleanly.
- Bounded valid snapshot lead to three ticks because a 30 Hz capture sent from
  `onBeforePatch()` can arrive before the matching 20 Hz state patch. The live
  integration test now passes through this actual validator instead of a raw listener.

QA completed:

- focused projection, candidate, protocol, and client-inbox tests passed;
- dedicated deterministic netcode gate: 37 tests passed;
- live two-client Colyseus integration passed with root-first same-space snapshots for
  both clients through the validating inbox;
- complete repository suite: 326 tests passed with no failures;
- optimized Next production build and TypeScript validation passed;
- source diff whitespace validation passed;
- live web and district health endpoints both returned HTTP 200.

No browser visual change is expected from M5 because the inbox does not drive rendering
until M6/M7. The executable wire, validation, retention, and teardown paths are covered.

## 14:02 CST - M6 Bounded Interaction-Island Selection

- Added shared circle, oriented-box, and mixed-shape current-contact geometry plus
  conservative swept time-to-contact admission. Server baseline ranking and client
  membership consume the same entity geometry instead of maintaining renderer-specific
  collision approximations.
- Replaced server broad-phase distance truncation with contact/TTC ranking before the
  64-entity protocol cap. This prevents dense nearby stationary ambience from excluding
  a slightly farther vehicle that is on an imminent collision course.
- Upgraded the interaction protocol to version 2 with a validated physical priority:
  player-controlled, mission-critical, or ambient. Remote occupied vehicles now rank as
  player-controlled while private traffic routes, AI plans, and mission state remain
  server-only.
- Added a renderer-independent stateful selector with a 32-point desktop budget and
  20-point mobile budget. Vehicles cost 4, props cost 2, humanoids/projectiles cost 1,
  and the controlled root consumes budget.
- Derived admission horizon from half RTT, adaptive interpolation delay, and doubled
  jitter, bounded to 100-500 ms with a 650 ms exit horizon. Current contacts outrank
  six-tick retained contacts and future TTC candidates.
- Added deterministic stable ordering, enter/exit hysteresis, lifecycle and collider
  revision invalidation, one-hop contact closure, and explicit conservative overflow.
  Closure cannot recursively pull an entire traffic queue into the island.
- Preserved destroyed vehicles as physical island obstacles and excluded dead humanoid
  bodies. Occupied vehicles, player-owned projectiles, and connected player bodies retain
  player-controlled priority.
- Composed one thin island controller in Phaser and Three over the public snapshot-source
  interface. The F3 network panel now reports island bodies, weighted points/budget,
  overflow count/points, horizon, and snapshot age.

QA completed:

- dedicated deterministic netcode gate: 55 tests passed;
- dense membership remained stable for 120 ticks under variable RTT/jitter and reversed
  input collection order;
- complete repository suite: 344 tests passed with no failures;
- isolated live two-client Colyseus integration passed in 19.8 seconds with validated
  protocol-v2 interaction snapshots;
- TypeScript validation, optimized Next production build, and source whitespace checks
  passed.

The in-app browser had no attachable local tab during this checkpoint, so no visual smoke
pass is claimed. M6 changes selection and diagnostics only; it does not yet alter dynamic
collision outcomes or replay. Whole-island restore/replay is the M7 checkpoint.

## 14:42 CST - M7 Deterministic Whole-Island Replay Foundation

- Added a bounded immutable island-state history that retains 24 same-tick frames and
  resets across controlled-root or static-world revision discontinuities.
- Added a generic renderer-independent replay coordinator. It restores every member from
  one authoritative baseline, advances fixed 30 Hz body steps in stable identity order,
  and evaluates every unordered dynamic pair exactly once in stable order.
- Added deterministic remote-intent continuation: hold the last server-applied command
  for two ticks, decay toward neutral over four ticks, then remain neutral. Hostile and
  non-finite values are clamped before replay.
- Added an explicit replay side-effect gate. Pure state transitions continue, while
  gameplay, durable transaction, one-shot presentation, and idempotent presentation
  effects are suppressed and counted. Nested replay and thrown kernels restore live mode
  correctly.
- Made replay fail closed for history-window overflow, world or entity revision changes,
  invalid commands, invalid family output, and kernel exceptions. Failed runs cannot
  publish partial island state.
- Added one shared Phaser/Three correction-smoothing policy. Canonical simulation state
  changes immediately; only position and wrapped-heading presentation offsets decay.
- Composed live history ownership beside the existing M6 selector and extended F3 network
  diagnostics with history size, replay count/ticks, p95 duration, pair steps, suppressed
  effects, and hard resets.
- Left family activation intentionally disabled. M7 captures live history and proves the
  generic replay machinery; M8 will inject and publish vehicle-to-vehicle contact output.

QA completed:

- dedicated deterministic netcode gate: 68 tests passed;
- exact maximum desktop envelope: 32 bodies over 24 ticks, 768 body steps, and 11,904
  stable pair evaluations;
- complete repository suite: 357 tests passed with no failures;
- isolated live two-client Colyseus integration passed in 18.8 seconds;
- TypeScript validation, optimized Next production build, and source whitespace checks
  passed.

The in-app browser still had no attachable tab, so no visual smoke pass is claimed. This
checkpoint does not change live vehicle collision outcomes; that remains the M8 vertical
slice.

## 15:55 CST - M8 Predicted Vehicle Interaction Islands

- Extracted the authoritative oriented-box dynamic-contact implementation into a shared
  deterministic kernel. Server and replay now use the same SAT overlap, mass-weighted
  separation, restitution impulse, projected speed, impact-zone, and damage semantics.
- Split the authoritative fixed vehicle tick into body and contact phases. All vehicles
  advance and update their broad-phase records before `finishTick()` resolves stable ID
  pairs, synchronizes occupants, and returns contact-moved bodies for reindexing.
- Removed the prior pair-order hazard where the first updated vehicle could collide with
  another vehicle's previous-tick transform. Added exact parity and insertion/update-order
  invariants around the server adapter.
- Extended whole-island replay with an immutable root trace, preparation hooks, and the
  authoritative baseline passed into publication. Family preparation can decline replay
  without mutating prediction.
- Added an atomic saved-vehicle replay handoff. It validates contiguous retained moves,
  replaces each historical predicted pose, installs the final canonical pose, retires
  the acknowledged prefix, and preserves newer pending input.
- Added the vehicle family adapter over the shared swept static-world step and shared
  oriented-box contact kernel. Local inputs replay by sequence; remote steering/throttle
  follows the bounded hold-and-decay policy; destroyed bodies remain fixed obstacles.
- Routed collision damage through the replay side-effect gate. Both vehicle damage
  callbacks are counted and suppressed during prediction, while the authoritative server
  remains the only owner of health, ignition, destruction, events, and durable effects.
- Published corrected local poses and promoted remote contact poses through both Phaser
  and Three. The matching authority acknowledgement cannot immediately undo island replay,
  and promoted remote state expires when newer server time arrives.
- Added a deterministic 150 ms RTT two-car impairment scenario. Predicted and authority
  contact ticks match with zero position and speed divergence.

QA completed:

- dedicated deterministic netcode gate: 74 tests passed;
- focused vehicle/contact gate: 21 tests passed;
- complete repository suite: 363 tests passed with no failures;
- isolated live two-client Colyseus integration passed in 18.9 seconds;
- TypeScript validation, optimized Next production build, and source whitespace checks
  passed;
- local web and district health endpoints returned HTTP 200;
- automated browser reached the NOCK0 shell with no console errors, but its fresh Privy
  session remained on the connection screen, so no automated driving visual claim is
  made for this checkpoint.

M8 is limited deliberately to vehicle-to-vehicle physical replay. Humanoid impacts,
entry/exit transitions, passenger constraints and attachments remain M9; projectile
correlation and historical hit validation remain M10.

## 16:40 CST - M9a Control-Epoch Contract

- Started M9 with the occupancy boundary because the existing snapshots identified a
  physical root but not the local player's relationship to it. In particular, a
  passenger promoted to driver could keep replay history captured under passenger
  semantics because the root vehicle ID did not change.
- Advanced the interaction wire contract to protocol v3. Every baseline now carries a
  validated `controlMode` (`on-foot`, `driver`, or `passenger`) and positive monotonic
  `controlRevision`.
- Added a dedicated per-player control tracker to the server snapshot projector. Entry,
  exit, seat changes, driver promotion, root replacement, and a discontinuous capture
  advance the epoch without coupling this policy to `DistrictRoom`.
- Made island history clear atomically across control-mode or epoch transitions. This
  prevents commands, remote intent, or collision state from one occupancy contract from
  being replayed under another.
- Added fail-closed protocol tests, projector transition coverage, and same-root
  passenger-to-driver history-reset coverage.

QA: `npm run test:netcode` passes 76/76 and `npx tsc --noEmit` passes.

## 17:09 CST - M9b Single-Root Attachment Presentation

- Added one renderer-neutral player attachment policy. It resolves an actor into one
  effective root plus body, passenger, weapon-origin, label, camera/minimap, and active
  collider semantics for on-foot, driver, and passenger modes.
- Replaced separate Phaser passenger, weapon, and nameplate anchor calculations with the
  shared attachment result. The public player pose now returns that same root.
- Replaced Three's duplicate passenger and attachment composition with the same policy.
  Three retains the resulting root on the rendered actor so camera, UI, debug, and later
  replay publication can consume exactly the transform used by the sprite and weapon.
- Routed the local minimap origin and local marker through the predicted attachment root
  in both renderers. The radar no longer trails the authoritative schema position while
  the visible local actor or vehicle is predicted ahead.
- Routed local debug humanoid colliders through the predicted root and removed duplicate
  humanoid colliders for occupied players. An occupied player's active physical collider
  is the vehicle OBB, matching server interaction projection.
- Confirmed vehicle lights and effects remain children of the predicted vehicle render
  root, while camera follow already consumes the renderer's player/vehicle pose.

QA: focused attachment/minimap/protocol tests pass 28/28, the complete suite passes 367
tests with no failures, and `npm run build` passes TypeScript plus the optimized Next
production build.
