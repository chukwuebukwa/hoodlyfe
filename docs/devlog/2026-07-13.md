# Devlog - 2026-07-13

## 07:38 CST - Interaction-Island Netcode Foundation

- Created `codex/interaction-island-netcode` from clean, current `origin/main` at
  `e48c622`.
- Added a shared pure vehicle simulation kernel for fixed-step handling, finite input
  normalization, damaged-engine speed limits, integration, and swept static collision.
- Replaced duplicated authoritative and browser vehicle handling with the shared kernel.
- Wired replicated `engineDamage` and `onFire` state into Phaser and Three saved-input
  prediction.
- Preserved server ownership of damage, events, occupants, AI traffic, and dynamic
  contacts.
- Replicated `serverTick` and added current, mean, and p95 correction diagnostics plus
  correction counts to the network debug projection.
- Added 10,000-step browser adapter parity coverage and 3,000 authoritative controller
  parity steps across all five vehicle models.
- Added the milestone and rollout plan in
  `docs/INTERACTION_ISLAND_IMPLEMENTATION_PLAN.md`.

QA completed:

- focused netcode and vehicle suite: 21 tests passed;
- complete repository suite: 301 tests passed;
- TypeScript type checking passed;
- optimized Next production build passed.

## 08:12 CST - Three Runtime Gate

- Loaded the live Three renderer at `http://localhost:5173/?renderer=three` against the
  local district server.
- Confirmed the world, local player, vehicles, mission UI, HUD, and minimap render
  together with no runtime exceptions.
- Confirmed all three canvases are visible: a 2560x1440 main backing surface rendered at
  1280x720, plus the minimap and HUD canvases.
- Captured the live main view and measured an 81.63% nonblack-pixel ratio and a 15.71%
  chromatic-pixel ratio; the center pixel was nonblack. This guards against the previous
  blank-canvas failure mode.
- Removed the now-unused server-local angle normalizer after shared-kernel extraction.

QA completed:

- Three runtime smoke passed at desktop viewport;
- browser console contained no application runtime errors;
- source diff whitespace validation passed.

## 08:34 CST - M1 Protocol Contract Checkpoint

- Added versioned wire contracts for unified player commands, applied-input receipts,
  immutable interaction snapshots, typed physical entity baselines, and last-applied
  remote intent.
- Added bounded command admission for monotonic sequence windows, client prediction
  ticks, controlled-entity identity, finite/clamped movement, normalized aim, known
  button bits, weapon slots, and provisional spawn IDs.
- Added fail-closed snapshot admission for server tick age, protocol and static-collision
  compatibility, entity capacity, lifecycle/collider revisions, finite physical state,
  stable identities, one space/layer, and intent-to-entity referential integrity.
- Runtime accepted values are reconstructed from validated fields and deeply frozen at
  the snapshot, collection, entity, intent, command, and spawn-ID boundaries.
- Split the implementation into wire contracts, command validation, snapshot validation,
  shared primitives, and a public barrel; no policy was added to `DistrictRoom`.
- Registered the complete M0-M11 interaction-island rollout as the active development
  goal. Each checkpoint will continue to record implementation and QA here.

QA status:

- protocol and deterministic vehicle tests: 7 passed;
- TypeScript type checking passed;
- the first aggregate run reported a transient failure in the existing two-client
  integration suite; a reporter-isolated rerun completed all 305 tests, including the
  19.6-second live multiplayer scenario;
- optimized Next production build and TypeScript validation passed;
- M1's fail-closed command/snapshot contract gate is complete.

## 08:52 CST - M0 Deterministic Network Gate

- Added four repeatable WebSocket-oriented impairment profiles: local, 75 ms regional,
  150 ms continental, and 250 ms intercontinental RTT with increasing jitter and
  0-1% simulated packet loss.
- Modeled packet loss as ordered retransmission and head-of-line delay because live game
  commands use reliable WebSockets; the harness does not silently delete commands as a
  UDP transport would.
- The scripted 12-second drive passes through real command admission, the shared vehicle
  kernel, immutable snapshot admission, saved-input reconciliation, and retained history.
- Added the reusable `npm run test:netcode` gate.
- The harness exposed and fixed command-level vector normalization: steering and throttle
  are now clamped independently at the protocol boundary, while on-foot vector
  normalization remains family-kernel policy.

Measured deterministic baseline:

| Profile | p95 correction | Maximum correction | Maximum pending | Resimulations | Retransmits |
|---|---:|---:|---:|---:|---:|
| Local | 0.00 px | 0.00 px | 0 | 0 | 0 |
| 75 ms | 13.67 px | 13.89 px | 6 | 55 | 1 |
| 150 ms | 13.67 px | 81.53 px | 9 | 74 | 2 |
| 250 ms | 77.00 px | 108.35 px | 17 | 93 | 4 |

All profiles remain below the 24-tick/800 ms retained-history budget and the 180 px
hard-correction threshold.

Final QA completed at 09:02 CST:

- dedicated netcode gate: 15 tests passed;
- complete repository suite: 307 tests passed;
- optimized Next production build and TypeScript validation passed;
- source diff whitespace validation passed;
- M0 is complete and ready for its remote checkpoint.

## 10:24 CST - M3 Saved On-Foot Prediction

- Replaced the presentation-only local movement replay with a 30 Hz saved-input
  predictor shared by Phaser and Three.
- Added a pure shared on-foot kernel for finite input normalization, analog/diagonal
  movement, melee locomotion policy, and independent-axis street/interior collision.
- Added bounded batched `on-foot.input` commands with monotonic sequences, a 24-command
  authoritative queue, and acknowledgements only after the server applies a move.
- Retained the legacy held-input path for vehicle and older-client compatibility without
  letting receipt masquerade as authoritative application.
- Added 96 moves of client history, baseline restore and pending-input replay, and hard
  correction for space transitions or errors above 120 px.
- Made the predicted body transform the single source for the local sprite, held weapon,
  nameplate, and debug collider in both renderers. This removes the floating-weapon and
  trailing-collider split caused by mixing predicted and replicated transforms.
- Kept interior entry/exit server-authoritative while sharing occupancy collision within
  the current space.
- Extended the network diagnostics with on-foot acknowledgement, pending-history,
  correction, and replay metrics; the debug panel now labels vehicle and foot values
  separately.
- Expanded `npm run test:netcode` to include the on-foot kernel, saved predictor, and
  deterministic impairment gate.

Measured on-foot impairment baseline:

| Profile | p95 correction | Maximum correction | Maximum pending | Maximum resimulated moves |
|---|---:|---:|---:|---:|
| Local | 0.00 px | 0.00 px | 0 | 0 |
| 75 ms | 0.00 px | 6.33 px | 8 | 1 |
| 150 ms | 0.00 px | 31.67 px | 10 | 1 |
| 250 ms | 0.00 px | 57.00 px | 23 | 3 |

QA completed:

- exact shared-kernel/authoritative parity over 10,000 movement ticks;
- no predicted wall crossing and no hard correction in all four impairment profiles;
- dedicated netcode gate: 23 tests passed;
- live two-client Colyseus integration passed with sequenced batch application,
  authoritative acknowledgement, and remote position replication;
- complete repository suite: 314 tests passed;
- optimized Next production build and TypeScript validation passed;
- local district server remained healthy on `http://localhost:2567`.

The in-app browser could not attach a fresh test tab during this checkpoint, so no new
visual smoke pass is claimed. The live protocol path was instead exercised through the
real server and two Colyseus clients; the browser attachment limitation remains a QA
environment issue, not a game-runtime pass.

## 11:54 CST - M4 Adaptive Remote Timelines

- Replaced the player-only arrival-time snapshot buffer with one bounded timestamped
  timeline for remote players, pedestrians, and vehicles in both Phaser and Three.
- Render sampling now targets estimated server time minus an adaptive 75-250 ms delay
  derived from p95 patch cadence, p95 jitter, median RTT, and p95 RTT variation.
- Added shortest-path angle interpolation, exact vehicle velocity projection, bounded
  100 ms extrapolation, speed and angular-velocity caps, hostile-value rejection, and
  history reset across teleports or long snapshot gaps.
- Centralized family policy for player, NPC, and vehicle teleport and extrapolation
  limits instead of embedding tuning in either renderer.
- Kept the local driver and local passenger vehicle paths off the remote timeline so a
  passenger camera, body attachment, weapon, and nameplate do not inherit network delay.
- Made each sampled body transform the shared anchor for remote body rotation, weapon,
  nameplate, passenger presentation, blood, fire, and vehicle effects in Three.
- Extended network quality diagnostics with p95 remote snapshot age, buffer underrun
  percentage, and extrapolation percentage.
- Removed the superseded `MotionSnapshotBuffer` implementation and tests.

Measured deterministic timeline baseline:

| Profile | Delay | p95 error | Maximum error | Underrun | Extrapolated |
|---|---:|---:|---:|---:|---:|
| Local | 100 ms | 0.03 px | 0.03 px | 0.0% | 0.0% |
| 75 ms | 100 ms | 0.03 px | 0.03 px | 0.0% | 0.7% |
| 150 ms | 115 ms | 0.02 px | 18.35 px | 0.6% | 37.6% |
| 250 ms | 195 ms | 0.03 px | 34.02 px | 1.7% | 21.3% |

QA completed:

- dedicated deterministic netcode gate: 29 tests passed;
- complete repository suite: 318 tests passed with no failures;
- optimized Next production build and TypeScript validation passed;
- source diff whitespace validation passed;
- live web and district health endpoints both returned HTTP 200.

The in-app browser again timed out while attaching a fresh local tab, so no new visual
smoke pass is claimed for M4. The executable test, build, live HTTP, and district-server
gates passed; visual browser attachment remains a tooling limitation.
