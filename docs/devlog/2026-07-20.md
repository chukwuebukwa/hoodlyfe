# 2026-07-20

## 01:26 CDT - World authoring worktree and immutable Play Draft

- Created worktree `/Users/jimmyjiggler/Documents/2026/opengta/nock0-world-authoring` on branch
  `codex/world-authoring-playtests` from `origin/main`.
- Centralized the editor IndexedDB schema and added an immutable playtest-revision store with a
  bounded per-district history.
- Added deterministic canonical document hashing, source compatibility fingerprints, and tests
  for content addressing and snapshot immutability.
- Changed the editor gamepad command to validate and snapshot the current in-memory document,
  persist it, and launch the exact revision in the district explorer.
- Added a fallback link to the last playtest revision so popup blocking does not strand a draft.
- Updated the explorer to use revision collision and spawn data while retaining streamed Three.js
  map geometry.
- Browser QA caught an ID mismatch between asset source `bil` and canonical world ID
  `industrial-district`. Added a revision-ID index, kept the route on `bil`, and retained the
  canonical ID inside the revision.
- Verified a fresh playtest tab rendered the nonblank Three.js district at the authored spawn,
  loaded 30 nearby chunks / 8,763 triangles, displayed revision `b0cf73e9`, and produced no browser
  warnings or errors.
- Added the unified world-authoring, runtime-pack, publishing, and scalable interior plan plus ADR
  0024.
- QA: focused Play Draft/collision tests passed `5/5`, TypeScript and the production Next.js build
  passed, and `git diff --check` passed. The full suite passed `561/562`; the unchanged
  `police-response-fleet-controller` reinforcement-count test expected 3 cruisers but received 2,
  and reproduced in isolation. No police or fleet files changed in this milestone.

## 04:14 CDT - Authoritative full-game Play Draft and lane topology QA

- Replaced the editor gamepad's lightweight geometry explorer for BIL with an authoritative
  `district-playtest` room that runs the normal Three.js client, player appearance, traffic,
  pedestrians, combat, collision, and all district systems against an immutable edited revision.
- Added signed, expiring, revision-bound playtest tickets and Railway bucket-backed revision
  storage, with a local `.nock0` development fallback excluded from Git.
- Made the explicitly enabled production editor temporarily public as requested. Publishing and
  playtest room creation remain disabled unless the editor service itself is enabled.
- Added a full-game `/playtest` route and a visible banner that distinguishes authored traffic
  lanes from the deliberate repository-lane fallback.
- Aligned live editor lane validation with the authoritative `LaneGraph` compiler. Exact
  off-corridor junctions now appear in the Validation drawer with their pixel offset instead of
  failing only after room creation.
- Replaced proximity-based junction authoring with exact polyline-intersection snapping. Corridor
  point edits now resnap connected junction topology, and the Validation drawer includes a
  `Repair junctions` command for older drafts.
- Diagnosed revision `f5c5e14e`: extending the two south-boulevard endpoints also shifted their Y
  coordinates by 16 px, creating shallow diagonals and leaving four existing junctions 3.1-5.1 px
  off-line. The repair command moved all four to their true intersections; the repaired revision
  compiled with zero warnings.
- Contract QA POSTed the repaired draft through the Next API, joined the resulting real Colyseus
  room, and observed one player, 27 vehicles, and five pedestrians. Focused editor/access/ticket
  tests pass `13/13`, and the TypeScript plus optimized Next production build passes. The full
  suite passes `567/569`; the existing long multiplayer integration scenario timed out and the
  existing police fleet reinforcement-count assertion failed, matching failures previously
  recorded before this milestone.

## 04:47 CDT - Railway Bucket activation and production request contract

- Merged the authoritative Play Draft branch into `main` on top of the gunplay magazine work and
  deployed the combined production build to the `hoodlyfe game` Railway service.
- Connected the new `indexed-cage` Railway Bucket through service-variable references for the
  endpoint, bucket, region, and S3 credentials; generated a dedicated Play Draft signing secret.
- Verified the public editor catalog changed from `storageEnabled: false` to `true` after the
  production redeploy.
- Production smoke testing exposed Express's 100 KB default JSON limit before a BIL document could
  reach the Next API. Raised the bounded JSON ceiling to 2 MB; the current repaired BIL revision is
  approximately 266 KB.
- A second production smoke test caught a combined-server ownership bug: Express consumed editor
  request bodies before Next could read them. Editor API streams now bypass the global parser and
  use a dedicated streaming parser that enforces the 2 MB ceiling before JSON decoding.
- Corrected the repository Railway deploy command to target the actual `hoodlyfe game` service and
  honor Git ignore rules, avoiding rejected uploads of local `.next` build output.
- QA: direct Railway Bucket PUT/GET passed in about 350 ms, focused editor tests pass `16/16`, and
  the TypeScript plus optimized Next production build passes.
- Final production QA stored repaired revision `e7545eb3371a03ae3fdf`, returned zero lane warnings,
  joined its signed `district-playtest` room, and observed one player, 28 vehicles, and five
  pedestrians running authoritatively from the bucket-backed draft.

## 05:36 CDT - Disconnected lane fallback diagnosis and fail-closed Play Drafts

- Pulled production revision `f45d4bed688cf312163f` directly from the Railway Bucket after its
  moved player spawn loaded correctly while traffic still stopped at the repository endpoints.
- Reproduced the authoritative compiler failure: only 150 of 156 directed lane nodes were mutually
  reachable. The six isolated nodes belong to `corridor-3`, a three-point, one-lane corridor with
  no junction into the main network. Its first point `(8488, 6240)` is eight pixels east of
  `central-avenue`, so the two polylines do not intersect.
- Confirmed that the server therefore loaded the draft collision/spawn while deliberately falling
  back to repository traffic lanes. The Play Draft banner exposed this as `REPOSITORY TRAFFIC
  LANES`, but the editor had not treated the topology failure as blocking.
- Added editor-side connected-component validation. Disconnected corridors now produce a blocking
  `corridor-disconnected` issue that selects and focuses the offending authored corridor.
- Removed repository-lane fallback from authoritative Play Draft creation. Runtime graph rejection
  now returns HTTP 422 with the exact `LaneGraph` compiler issue and does not open a misleading room.
- QA: the exact broken production document now yields the actionable `corridor-3` validation issue;
  API contract testing returns HTTP 422 with the 150/156 connectivity error. Focused lane/editor
  tests pass 14/14 and TypeScript passes.

## 06:36 CDT - Weapon presentation checkpoint and one-way lane requirement

- Checkpointed the pending custom pistol, SMG, and shotgun presentation pipeline: catalog-owned
  raster asset paths and hand anchors now drive Three.js held models and HUD icons from the same
  definitions, with focused rendering and HUD expectations updated alongside the new assets.
- Recorded explicit single-direction corridor support as a required follow-up. Directionality must
  be authored once and consumed by compilation, graph validation, junction movements, routing,
  spawning, and editor previews; it must not be implemented as a client-only visualization option.
